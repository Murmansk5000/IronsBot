# QQ 官方机器人可用性验收

2026-09-15 用户重新明确优先目标：先在官方机器人上可用，不继续以结构重构代替使用验收。
旧的固定 98% 停止使用。完整多平台目标仍保留，但不得用 OneBot 内部清理阻塞官方单账号验收。

## 当前结论

2026-09-21 当前 Unraid 容器运行镜像修订为 `ae186314`，清单 digest 为
`sha256:a629bb7be846c32db312f632e4e0a1d6a337897bb47572cc80db656df42be87a`。对应公开流水线
`35583061598` 已通过并发布标签 `0.5.1.1970` 与 `sha-ae18631`；镜像展开大小为
`260096148` bytes，`/app`、site-packages 和字体分别为 `4860 KiB`、`106000 KiB` 和
`19452 KiB`。生产严格配置加载、五个官方账号 READY、OneBot 静默观察、3/3 无头账号和
应用启动均通过，部署后检查未发现 ERROR、CRITICAL、Traceback 或 ValidationError。

`c328a103` 仍是下列已完成真实功能验收的证据基线，而不是当前运行修订。该候选的公开仓库
发布流水线 `35528052167`、依赖审计、镜像冒烟、体积门禁及 Docker Hub/GHCR 发布均通过；
当时严格配置检查、五个官方账号 READY、OneBot 静默观察和三个无头账号也均已验证。

该修订删除 QQ Official 投递中“只寻址第一段回复”的状态。受控群真实阵容查询依次取得
三次被动送达：`sequence=1` 的进度正文和 `sequence=2` 的结果摘要都在同一条消息内包含
发令成员提及、换行和正文，`sequence=3` 只包含图片，没有单独的提及消息。NapCat 客户端
观察与腾讯送达日志一致，OneBot 没有发送业务回复。该记录复核 A6 的三段送达，并补齐
B10 对一个请求内所有文本段的服务端与客户端证据。用户随后在手机 QQ 确认最终视觉正确：
提及与正文同条、提及后正确换行、正文没有重复。

受控群实机先后发送“@机器人 帮助”和数字 `3`。两次均由同一官方账号认领，并各自只投递
一条 `mode=passive`、`sequence=1` 的 Markdown 回复；NapCat 观察到的 Markdown 源内容以
成员提及、换行、正文开头，没有独立提及消息或重复正文。主帮助与“赛尔号查询”二级帮助
都以转义后的 `0. 【退出】` 收尾，避免 QQ Markdown 将零号项续排。服务端与 NapCat 证据
已经通过；手机 QQ 对换行和最终视觉对齐也已由用户确认。

前一精确镜像 `e6b8bdad` 由已授权的 NapCat 测试身份向公众账号发起 C2C 复核。帮助、明确玩家、
竞技段位榜指定名次、精灵详情、刻印详情和新增内容均由 `public_bot` 唯一认领并取得腾讯
被动回复回执；NapCat 实际收到玩家文本、榜单文本、精灵图片、新增内容图片，以及刻印的
图片与说明。刻印两段依次使用 `sequence=1/2`。多轮“雷伊立绘 → 1”先返回包含零号退出的
候选菜单，再返回图片与标题，且没有被前一条玩家绑定菜单吞掉。该记录在 `e6b8bdad` digest 上
复核 A1、A2、A8、A10、A11、A12 和数字选择会话；A3-A7、A9 仍保留为待后续复核。

2026-09-20 当前生产运行镜像修订为 `8729bb14`，清单 digest 为
`sha256:c2a97fab1b97ab3412b59880a2d750f01ed47f26d141a019ce0bf9a75afab9e5`。公开仓库发布流水线
`35519120670` 的依赖审计、候选镜像冒烟、体积门禁和 Docker Hub/GHCR 发布全部通过。
私有 Preview 的完整提交历史和有效 Release 内容已经归入公开仓库，私有仓库已 Archived，
不再参与开发或发布。该精确 digest 已部署到 Unraid：严格配置加载通过，三个官方账号进入
READY，OneBot 继续保持静默观察。生产配置在修改前保存了仅 root 可读的时间戳备份。

该版本新增确定性的多官方账号入站所有权：默认账号负责私聊和未分配群，逻辑群只有一个
活跃官方端点时由该端点自动接管，多端点群可用显式路由消除歧义。受控群在 23:24 的真实
“阵容 + 成员提及”验证中，指定账号识别并执行一次，另一账号在业务分发前记录脱敏的
`ignored by account routing`；进度、玩家说明和图片均成功送达，没有第二份“尚未绑定”回复。
同一验证也证明重构前的 OneBot 玩家绑定可在可信官方身份映射后复用。已有映射会在启动时
事务性收口；新映射建立时，如果只有官方直属重绑记录，则迁移到 OneBot 数字主身份并删除
重复直属记录。无法由数据库证明的数字账号仍不会依赖昵称、头像或时间猜测身份。

同日权限审计确认帮助和戳一戳必须描述群整体开启的 Feature，而命令执行允许配置的
超级管理员 bypass。提交 `9aa4ee79` 将两者拆为显式目录入口，并覆盖 OneBot、QQ
Official、普通成员、关闭 bypass、命令 audience 与黑名单优先级。该提交已经进入
`33631b0a` 发布镜像，但客户端权限矩阵尚未完整实测，不能由发布事实代替行为验收。

2026-09-16 用户要求停止本机机器人实测，先完成代码功能，后续统一部署到 Unraid
再做客户端验收。因此本阶段不再重启本机进程、不建立新的腾讯连接，也不把等待用户
发送口令作为代码工作的阻塞条件。实机验收仍按下方固定 8 项里程碑记录；保守基线为 5/8，
代码侧完成度与实机完成度分开陈述，不互相冒充。历史查询和交互证据在新 digest 上只按
受影响行复验，未复验的整组不能因旧镜像通过而继续计入完成数。

本轮重新从原版与当前仓库的真实 `PluginContribution.commands` 导出命令目录，检查了
219 个无需生产别名的原版具体口令。`米米号` 与 `新增内容` 由当前更精确的共享入口认领；
`战队玩家1` 需要配置中的玩家别名解析，其余具体口令均由当前目录认领。QQ 官方路由
构造仍强制每个已加载直接命令都具有 portable operation，缺少实现会在启动装配阶段
直接报错，不能静默出现在帮助中但执行无响应。

玩家与二级栏目、扩展入口、榜单参数、候选菜单、图片和新增内容、B站、配置消息、
战队、AI 及 QQ 官方投递链的聚焦回归通过。最新全量结果为 3893 passed、7 skipped；
Ruff lint/format、生产与测试 BasedPyright、compileall、`scripts/check_repo.py --static` 和
diff check 全部通过。公共仓库的共享功能实现与自动化守护已收口；阵容的实际封包解析和最终渲染
仍按既定架构由私有扩展制品提供，公共端只负责稳定扩展契约与跨平台执行入口。

同日重新对照原版 OneBot 输入规则与当前共享路由：原版明确拒绝把 `BOT_MENTION`
交给 `explicit_command`，所以 `@机器人 + 指令` 会先被 AI 或提示 matcher 消费。当前实现
只放宽这一点：直接指令与 `@机器人 + 指令` 都先由同一命令目录认领；只有未认领的
`@机器人 + 非指令` 才在 AI 聊天与帮助提示之间选择。未 @ 的普通文本继续保留原版
AI 意图或静默语义。提交 `6028b63b` 为 direct/mentioned 两种输入增加同一业务 operation
断言，并要求 AI 意图与聊天调用次数均为零；扩大回归 120 项及上述全量门禁通过。
该提交只修改测试和架构说明，不改变当前生产镜像或实机验收 digest。

22:22 的同一生产 digest 实测补充了全量群消息模式的反例：直接发送“帮助”以
`GROUP_MESSAGE_CREATE` 到达、被识别并成功回复；随后 `@机器人 + 帮助` 与
`@机器人 + 非指令` 也以 `GROUP_MESSAGE_CREATE` 到达，却都被错误分类为 direct 且
`recognized=False`。原因是适配器仅凭 `GROUP_AT_MESSAGE_CREATE` 事件名判断机器人
提及，没有使用全量事件携带的结构化 `mentions[].is_you`，也没有从命令正文中移除该
结构化提及。目标行为因此明确为：全量事件中的 `is_you=true` 与专用 @ 事件具有相同
寻址语义；只移除对应机器人的提及，保留其他成员提及；普通全量群消息仍禁止自动 AI
兜底。修复发布后，直接命令、@命令、@非命令的 AI/提示分支必须在新 digest 上重测。

部署说明同时修正：`SEER_PASSWORD_<player_id>` 必须填写明文密码，IronsBot 只在内存中
生成登录 MD5；`.env.example` 与 Unraid 中文模板已统一并增加守护测试。此项不改变 TOML
schema 或环境变量名。

Unraid 发布链也已收口：提交 `3fa39660` 让 Dockerfile 与正式发布工作流默认安装
`qq-official` extra，同一个 `latest` 镜像可同时承载 OneBot/NapCat 与腾讯官方 SDK；
`e3515420` 在候选镜像 smoke 中离线导入腾讯 SDK 和 IronsBot 官方运行时，并为这次明确的
统一运行时依赖增量设置受控镜像增长预算；`5f51f3ed` 补齐 Docker 与根 README 的完整
凭据示例。以上检查不启动机器人、不连接腾讯，也不代替 Unraid 实机验收。

基础接入已有真实使用证据，不能说机器人完全不可用；最新代码的完整日用能力尚未验收。
旧的 12:52:06 进程已收到正常 Ctrl+C，确认旧进程链退出后才启动新实例。
本机于 17:50:41 启动提交 789bccba（最新业务提交 c26ba863），17:50:47 获取令牌、进入 READY 并完成应用启动。
本地 HTTP 端口可响应；根路径返回 404 只证明服务响应，不代表业务健康检查通过。
启动仍有 Fontconfig 和 SQLAlchemy 关系警告；图片是否正常需要官方客户端复验。
最近全量本地结果为 3728 passed、7 skipped；这不是 QQ 客户端功能验收结果。

本地测试配置严格加载通过；开发账号最初只启用 about、help、seer_data、seer_peak、seer_pet。
玩家账号和无头登录账号均为 0，private_extensions 未启用，自定义键盘未启用。
因此完整玩家查询、榜单和阵容等能力不能算作已部署验收；需确认完整配置来源和依赖，不能仅打开开关就宣称可用。
已请用户测试“帮助”“新增内容”“赵云立绘”及回复序号“1”，并提供完整配置的本地文件路径，无需粘贴凭据。
本轮未修改本地 TOML 或环境文件，未发送真实测试消息，等待客户端结果。

后续用户反馈“新增内容不是图片”：共享操作确实只构造文本菜单，没有调用已有图片渲染端口。
已修复根菜单和分类菜单，继续复用通用会话与媒体发送；本地相关 69 项测试通过，不等于客户端已显示图片。
18:05:05 已正常替换本机开发进程，加载修复提交 1aca1f7b；18:05:11 READY 并完成应用启动。
官方账号当前用户别名、群别名、私聊超级管理员及群超级管理员配置均为空；未通过隐式身份映射添加任何授权。

19:09:04 已用提交 42245cc2 再次启动本机开发进程。NoneBot 显示
`Current Env: dev`，随后官方账号正常取得 Access Token 并进入 READY；TOML 的
开发环境值不再只停留在 IronsBot 配置模型中。该提交全量验证为 3731 passed、
7 skipped，Ruff、生产与测试 BasedPyright、compileall 和静态仓库检查通过。
这条记录只证明当前 HEAD 的启动与环境选择，不替代图片及命令实机验收。

用户提供原版 TOML 作为本机开发配置的参考，只保留 owner、pjx 用户以及 admin、ouc 群的功能。
这不是生产配置迁移，不修改生产或 Unraid。参考配置中的多消息回复、部分 B站设置等与当前 schema 不同，不可整份覆盖。
用户同意同时确认官方身份；仅保留功能策略意图，不能把原 QQ 号、群号作为 OpenID，也不能按昵称猜测后授予管理员权限。
当前四份策略意图：owner 为 all + admin_notice；pjx 为 all_push + lucky_skin_window；admin 为 all；ouc 为 standard + meeting + lucky_skin_window + player_lineup_private + chuchu_3。
官方身份尚待显式确认，开发配置尚未应用这些授权。

为避免从日志、昵称或消息时间猜测身份，开发账号可临时显式启用
`qq_official_identity_info`，再由目标用户私聊发送“官方身份”，或在目标群 @机器人
发送“官方身份”。该命令只返回当前事件携带的官方标识，不写日志、不建立 QQ 号关联，
也不会被 `all` 功能包隐式启用；四个身份仍须逐一从目标会话确认。

19:41 已把用户说明的原版 TOML 作为本机开发配置迁移，而非生产或 Unraid 配置。
迁移保留全局业务设置、18 个玩家账号元数据、7 个配置命令和 1 个定时任务；OneBot
身份只保留 `owner`、`pjx`、`admin`、`ouc`，它们不会被当作官方 OpenID。开发配置改用
`full` 插件清单，关闭仅等待 NapCat 的 `bot_routing`，并关闭会下载不符合当前发布契约
数据的启动同步。迁移后的严格配置加载通过，官方账号重新获取令牌并进入 `READY`。

本机没有任何 `SEER_PASSWORD_*` 开发变量，因此无头账号、私有阵容扩展和幸运橱窗
暂时保持关闭；这三项不能因旧 TOML 中曾经启用就记为可用。恢复时必须由部署者提供
对应开发凭据，不能把密码写入 TOML。官方端 `owner`、`pjx`、`admin`、`ouc` 权限也仍
等待各自会话执行“官方身份”后显式写入；旧 QQ 号和群号继续仅属于 OneBot 配置。

审查原开发配置时确认配置消息序列曾在重构中从 `messages` 退回单条 `message`，会丢失 chuchu_3 等连续回复。
现已恢复唯一的 `messages` 数组结构，覆盖命令、关键词回复和定时推送；不保留单数旧字段双读。

19:53 用户在提交 `36111074` 运行的最新开发进程中私聊发送“官方身份”。运行时将其识别为
`C2C_MESSAGE_CREATE` 的 direct 命令，腾讯接口接受了 `sequence=1` 的被动文本回复；同次请求
还完成了一次 Access Token 刷新。日志只保留账号别名和结果分类，没有记录 OpenID。该命令
只向发起会话回显当前官方身份，尚未自动授予 `owner` 权限；管理员映射仍等待用户把机器人
回显值明确交给本机开发配置。此证据证明当前 HEAD 的官方私聊路由与文本投递可用，但不替代
最新版图片复验。

20:05 对提交 `ba698e81` 执行了不启动第二条腾讯连接的完整资源生命周期冒烟：先运行应用
拥有的 `data_sync` 资源钩子加载当前开发数据库，再以 QQ Official 的平台无关入站模型调用
共享路由。“帮助”生成 4 个二级选项；“新增内容”生成二进制图片和 17 个交互选项；
“赵云立绘”生成 7 个候选，选择后返回二进制图片与文字。该检查证明当前开发数据、业务
路由、菜单与渲染链可用，并在结束时关闭 HTTP、数据库和任务资源；它没有调用腾讯上传
接口，因此客户端媒体上传与显示仍必须由当前 READY 进程复验。

SDK 1.2.2 的官方示例随后证实 `MSG_TYPE_QUOTE` 是用户引用消息，不是机器人自己的回包。
此前运行时会把这类事件全部丢弃，用户点 QQ 客户端“回复”后发送命令或菜单序号会静默
无响应。当前实现已删除该过滤，保留引用标识作为 `reply_to_id` 元数据，并让新输入继续
经过持久去重、Feature、命令和会话路由；专项 79 项测试通过。真实客户端仍需用“引用
菜单消息后回复序号”复验。

实机复验期间还确认，SDK 消息回调若因字段不完整而无法解析，旧运行时会在写入任何
入站日志前静默返回，无法与“腾讯没有投递事件”区分。当前运行时会为这种情况记录
脱敏的 parser rejection，只包含账号别名和事件类型，不记录消息正文、AppID、消息 ID
或 OpenID；成功解析的事件仍由既有 routed 日志记录。该诊断不改变命令行为。

本机开发配置随后按 `docs/examples/fixed-images.toml` 安装五项固定图片命令，五张原版
PNG 仅复制到忽略的 `data/sendpic` 部署目录，没有重新打包或提交。服务逐项读取并验证
PNG 签名成功，共 5 项；开发账号临时启用 `image` 后，提交 `3dff0cdd` 于 20:29 再次
取得 Access Token、进入 READY 并完成启动。该记录只证明本地配置和资源可用，腾讯
客户端的图片上传与显示仍须用“学习力”或“巅峰姬”实测。

玩家主查询的共享会话还补上了加载期数字输入预留：用户在米米号资料尚未完成时发送
序号，输入会等待首条菜单真正送达后再执行，不会抢在菜单前返回详情；查询异常或首条
发送失败会唤醒并取消等待，同时删除未展示的菜单；等待也受同一会话 TTL 约束，不会
因首个查询卡住而永久挂起。198 项 portable、QQ Official 和
OneBot 适配回归通过。真实客户端的快速回复与引用回复仍须分别验收。

通用菜单随后加入显式的群内只读共享策略。OneBot 只在用户引用当前机器人菜单
时转交共享选择；会话服务为回复者克隆独立上下文，并在执行玩家栏目时按回复者
当前 Feature 重新授权。绑定和换绑选项不在共享集合中，原菜单也不会被其他成员
关闭。相关 261 项本地回归通过；这为删除 OneBot 玩家专用详情会话补齐了最后一个
行为门槛，但不替代官方客户端的引用回复实测。

OneBot 玩家主查询、绑定和解绑随后全部改为执行同一组 portable operation，旧的
`player_detail_conversation.py`、`player_context.py` 以及四个只验证旧状态字典和
专用 handler 的测试文件已删除。加载期输入、共享引用、Feature 重验、绑定隔离、
语义准入、首条发送失败和后台缓存分别由公共会话、平台适配与 service 测试覆盖。
相关 459 项及全量 3712 项回归通过，另有 7 项按条件跳过；生产改动净删除约
800 行。不涉及 TOML、env、Docker 或 Unraid 字段。该结构收口不增加官方实机
验收进度。

21:18 清除本机持久化 WebSocket session 后重新 Identify，Access Token 获取、READY
和应用启动均成功。清理前后用户各发送一次消息，网关序号和入站去重库均未变化，
运行日志也没有 routed、parser rejected 或 callback failure，因此这两条消息没有
到达本机 SDK 连接，不属于命令解析失败。需从同一官方机器人私聊，或在已配置测试
群内 @机器人重新发送后继续定位平台投递范围；不得把本次未投递记为命令通过。

21:40 以当前开发数据库直接调用共享刻印服务，“V8”返回 12 个候选，证明数据与
解析链可用；此前 portable 冒烟无回复是因为开发账号未启用 `seer_mintmark`，不是
官方适配器或刻印解析缺陷。本机忽略的开发配置随后改为
`help + about + standard + qq_official_identity_info`，严格解析确认帮助、图片、精灵、
刻印和榜单均已授权。21:43 新进程取得 Access Token、进入 READY 并完成启动。
该本地配置不提交、不影响生产、Docker 或 Unraid；客户端仍需重新发送命令验收。

21:46 对扩展后的开发账号执行不连接腾讯的批量共享路由冒烟：帮助返回 10 个
二级入口，新增内容、竞技池、大师池、专家池返回图片；精灵、立绘、刻印、宝石、
装备、属性、群星牌和榜单均被认领并返回结果。玩家基础查询在没有无头密码时也能
返回文本；缺少 `SEER_PASSWORD_*` 只阻止必须登录的阵容、橱窗等能力。随后重新
比较原版与当前测试命令目录：原版 81 项、当前 79 项、同 ID 72 项；旧大师池 ID 已
合并，8 个固定图片口令由本机 5 项通用图片配置全部覆盖。这些仍是本地证据，不替代
腾讯客户端上传、菜单和交互验收。

21:55 当前开发进程收到一条 `GROUP_AT_MESSAGE_CREATE`，识别为机器人提及输入并
成功路由；腾讯接口接受 `sequence=1` 的群聊被动文本回复。脱敏日志未保存正文，
因此该证据只确认最新提交上的群 @ 入站、命令识别和文本投递，不推断具体业务口令，
也不替代图片上传或多轮菜单验收。

跨平台目标配置不再要求为同一个真实群创建 `admin` 与 `official_admin` 两套别名。
群和用户别名统一位于 `[identities]`：全局 Feature policy 同时覆盖其 OneBot 与官方
端点；普通群成员由静默 NapCat 观察自动关联，不再逐项写入 TOML。
这属于权限与目标模型收口，不增加业务功能，也不替代任何客户端验收项。

22:21 使用首版逻辑用户映射启动时，官方账号已取得 Access Token 并进入 READY，但
messaging 可选生命周期因把群成员端点枚举成私聊目标而降级。该问题不是腾讯连接失败；
Feature service 已新增只返回可私聊 user 端点的明确接口，定时消息、活动和 B站私聊
发送方全部改用该接口。配置同时增加原生端点唯一性校验，禁止同一个群号、QQ 号或
同一官方作用域 OpenID 被多个逻辑别名占用。修复后的启动仍需重新验证后才能记为健康。

22:26 修复版重新取得 Access Token、进入 READY，messaging、活动调度及其余启动钩子
全部完成，没有再次出现群成员被转成私聊会话的异常。随后 OneBot 不在线的正常发送失败
暴露出路由日志仍打印原始 QQ 目标；该日志路径已改用不可逆引用摘要并增加回归测试。

22:29 当前修复提交重新取得 Access Token、进入 READY 并完成全部启动钩子。22:30 的
OneBot 不在线告警只包含不可逆的 `target_ref` 和 `bot_ref` 摘要，没有再输出原始账号或
目标标识；messaging 和活动调度保持健康，日志隐私修复已完成运行时复验。

逻辑用户 `owner` 是当前唯一的超级管理员身份。全局 `bot.superusers = ["owner"]` 通过
显式别名解析到该用户已经配置的 OneBot 和 QQ 官方群成员端点；不同平台标识不是多个
管理员，也不通过昵称、头像或消息时间推断关联。群成员 OpenID 仍只在所属 AppID 和群
作用域内有效，不能用于官方私聊；后续取得 C2C `user_openid` 时，应继续挂到同一个
`owner` 别名，而不是再次声明超级管理员。

22:38 和 22:46 当前开发配置各完成一次官方群 @ 图片命令：入站均被识别，腾讯群媒体
接口接受 `sequence=1` 的 `QQOfficialImagePayload`，随后记录被动回复送达。22:45 还完成
一次 C2C 文本命令的 `sequence=1` 被动回复。以上证明最新业务链路已调用腾讯上传和发送
接口，但客户端是否实际显示图片仍需用户确认，不能仅凭服务端成功记录记为通过。

22:38 的 B站菜单实测暴露出业务日志直接格式化 `ActorRef` 和 `ConversationRef`，会记录
传输身份。提交 `e23880b2` 将 B站、AI、玩家阵容、榜单、幸运橱窗及 OneBot 群探测、
排队会话、限流、红包、审核和手动刷新日志统一改为不可逆摘要，并新增全仓传输日志
结构守护。相关 222 项回归、Ruff、BasedPyright、compileall 和静态仓库检查通过。
22:46 旧进程完成正常关闭；22:47 新提交重新取得 Access Token、进入 READY 并完成
全部启动钩子。修复后的真实 B站菜单日志仍待下一条客户端命令复验。

22:54 本机开发配置增加一个被 Git 忽略的无头查询账号，米米号只写本地 TOML，密码只写
`.env.dev`；严格配置加载确认查询池包含一个凭据完整账号。重启后完成登录认证并进入游戏
服务器，查询池报告 `healthy=1 / configured=1`。这项配置没有修改 SeerAPI；运行时继续
读取当前发布的 `seerapi-data.sqlite`，网络目录中的 SeerAPI Git bundle 仅用于恢复数据
构建源码。

22:56 当前官方群玩家基础资料命令通过 `owner` 超级管理员策略，单个无头 worker 依次完成
4 个实时封包，约 4 秒后由 QQ 官方群被动文本回复送达；随后一条引用回复也被识别并回复。
后台收集、巅峰和群星牌预热继续执行，部分榜单页和巅峰模式超时并按既有部分结果策略
降级，没有阻止基础资料首屏。该记录证明玩家基础实时链路可用；收集、巅峰、群星牌和
榜单的完整客户端结果仍需分别验收。

23:10 重新核对阵容部署链：公共仓库中的无头查询、缓存、发布数据解析、渲染端口和
跨平台动作注册契约均存在，但实际阵容封包解析与最终渲染仍由独立私有扩展制品提供。
本机开发配置未启用私有扩展，安装缓存不存在，Docker daemon 当前不可用，本地也没有
私有仓库拉取凭据；对私有镜像的匿名 manifest 请求被仓库拒绝。已在线的公共无头账号
不能替代缺失的私有协议实现。因此阵容当前属于明确的部署制品门槛，不是 QQ 官方路由
或无头登录缺陷；取得私有镜像拉取权限并完成扩展安装前，不得把阵容记为已验收。

23:19 源码启动复验发现应用此前没有在严格配置加载前读取 `.env.dev`，旧进程依赖终端
手工注入的环境变量。提交 `a4df5838` 建立唯一 dotenv 入口，按“进程/容器变量、环境
专用文件、基础文件”的顺序加载。随后只设置 `ENVIRONMENT=dev` 即可自动取得本地
Secret 和无头密码，官方账号再次获取 Access Token、进入 READY，无头 worker 登录成功。
该修复不改变 Docker/Unraid 已注入变量的优先级，也没有把密钥写入 TOML 或日志。

23:22 正常关闭上述实例时，提交 `d528fb90` 已使 APScheduler 的 shutdown 取消安静结束，
没有再产生任务异常日志。提交 `b2b2e83a`、`d47d14d1` 随后将无头登录、心跳、断线、
重连、榜单刷新和 worker pool 汇总中的登录账号统一替换为不可逆摘要，并扩展 AST 日志
守护以覆盖间接字符串拼接。23:23 最新实例再次完成 Access Token、READY、无头登录和
全部启动钩子，日志只包含账号别名、worker 数量和摘要。客户端图片显示与修复后的 B站
菜单日志仍待复验，因此最新部署里程碑继续保持“进行中”。

23:27 使用明显无效、无法对应真实会话的目标执行一次受控主动群消息请求。腾讯接口未
投递消息，返回 HTTP 400、API code `11255` 和 trace id；`QQOfficialHttpClient` 保留了
这些结构化字段，发送层将结果分类为永久失败。腾讯公开文档没有检索到该 code 的稳定
释义，因此这里只记录实测字段，不推断它等同于额度耗尽或特定权限错误。本次证明真实
API 错误不会被吞掉，也不会表现成成功；主动消息额度和真实未授权目标仍需独立验收。

23:52 正在运行的 QQ 官方连接收到网关 `4009 Session timed out`，运行时先将账号标记为
断开并使待处理操作失败，随后重新连接并于 3 秒内再次收到 READY；没有重启应用，也没有
留下第二个官方连接。00:02 无头游戏连接也在心跳期间断开，熔断器开启并按无限重试策略
调度，约 11 秒后重新认证、进入游戏服务器并恢复健康。这两条是当前本机进程的真实自动
恢复证据，不等同于观察到了腾讯重复投递或多 AppID 隔离。

上述无头封包错误的回溯仍通过 `SocketRecvError.head` 打印了登录账号，绕过了此前的日志
脱敏。提交 `2653e042` 删除异常对象中的完整封包头，只保留错误码和命令号；服务层改读
明确的 `result_code`，封包调试日志也不再格式化完整头对象，静态守护新增 `head/headinfo`
禁记规则。相关 73 项测试、Ruff、BasedPyright、compileall 和 diff check 通过。

00:10 用 `2653e042` 正常关闭旧实例后重新启动；开发环境文件自动加载，官方账号重新取得
Access Token、进入 READY，无头 worker 登录成功，所有启动钩子完成。最新版客户端图片
显示和修复后的 B站命令日志仍未获得用户侧证据，因此最新部署里程碑继续保持进行中。

01:03 用户从官方私聊明确提供了 owner 的 `user_openid`，并确认它与已配置的 admin 群
成员身份属于同一逻辑用户。本机开发配置已在账号级 `user_aliases` 下补充 owner；原群成员
映射继续保持群作用域，程序没有根据昵称、头像或时间推测身份。该配置被 Git 忽略，原始
OpenID 未写入仓库、测试或日志。

该时点曾将 QQ 官方 AppID 与 Secret 都移入按账号别名命名的环境变量。后续目标 schema
已收口为：公开 `app_id` 写入对应 TOML 账号，只有 AppSecret 使用
`APP_SECRET_<AppID>`；TOML 中的 `secret` 与旧的两类账号别名环境变量都会直接报错，
不提供兼容双读。README、完整示例、Docker 文档、Unraid 中文模板和多账号协议说明均以
后续目标 schema 为准。

01:19 使用提交 `027c1d03` 启动最新源码。本机先下载当前 SeerAPI 正式发布库并验证发布方
SHA256，再在独立候选库中应用尚未协调发布的 metadata v2 契约；SeerAPI 生产端校验器确认
149 张表及 schema 指纹，IronsBot 消费端也确认契约版本、必需表、字段和指纹。旧进程正常
关闭后才原子替换数据库，没有保留 v1 兼容副本。新实例严格加载环境凭据，取得 Access
Token、进入 READY，无头 worker 登录并报告 `healthy=1 / configured=1`，全部启动钩子
完成。最新版客户端图片显示或 B站命令日志仍未复验，因此本项继续保持“进行中”。

01:20 至 01:21，用户按给出的回归顺序在官方群发送竞技池图片查询和 B站账号查询。
脱敏运行日志不保留正文，因此命令与响应的对应关系由发送顺序和响应类型推断：两条
`GROUP_AT_MESSAGE_CREATE` 均被识别；第一条最终由腾讯接口接受群聊
`QQOfficialImagePayload`，第二条约 1 秒内接受 `QQOfficialTextPayload`，两者均为被动
回复 `sequence=1`。同一时段文件日志共 10 行，未检出完整 OpenID 或长数字身份。
这满足最新版 B站路径及部署健康回归；图片接口已接受，客户端实际显示仍作为主要查询
验收中的呈现证据继续确认。

01:26 起用户在官方群继续执行玩家资料、详情菜单、立绘、刻印、新增内容和榜单批次。
玩家基础资料约 3 秒完成并送达；后续输入均被识别，刻印候选选择实际产生图片及说明，
用户确认客户端可见，但原版单气泡的“标题 + 图片 + 详情”被官方接口拆成三条消息。
同批次还发现纯图片的新增内容菜单因为携带 Prompt 而在发送前被拒绝，错误被明确记录为
`unsupported_message`，没有静默伪装成功。

提交 `f05afd75` 在 QQ 官方传输边界统一处理这两类问题：前后都有文字的图文详情压缩为
“图片 + 完整说明”两条；未启用官方键盘能力时，图片菜单不再要求文本 payload，数字
会话照常保留；启用键盘能力时才增加一条带完整选项的文本 payload。腾讯当前接口以
`msg_type=0` 发送文本、以 `msg_type=7` 发送富媒体，不能复刻 OneBot 单条消息中的图文段
混排，因此两条是保留原图和可复制说明时的最小数量。修复后本机重新取得 Access Token、
进入 READY，无头 worker 健康；全量验证为 3747 passed、7 skipped，最新版客户端复验
仍待完成。

10:48 使用当前 `776406ce` 启动本机开发实例。账号在 10:48:22 获取 Access Token，
10:48:23 进入 READY；无头查询池随后报告 `healthy=1 / configured=1`，10:48:29 完成
全部应用启动钩子。进程持续运行，8080 端口响应；根路径 404 只作为 HTTP 监听证据，
不作为业务健康接口。短时检查未发现应用错误或 degraded 资源；NapCat 未连接时一次
B站私聊通知明确返回结构化 `bot_unavailable`，没有影响 QQ 官方账号。启动仍有既有
Fontconfig 告警。该提交新增大师模式全服玩家榜并接入玩家“巅峰”摘要，本地全量验证为
3760 passed、7 skipped；大师榜及修复后的紧凑图文仍须由当前客户端命令确认，不能因
本地测试或 READY 自动记为主要查询通过。

同一当前开发配置随后完成一次不连接第二条腾讯 WebSocket 的权限编译审计。逻辑用户
owner 的官方私聊端点和 admin 群成员端点均被识别为同一个超级管理员；admin 群允许
玩家与榜单功能。ouc 群允许配置的标准榜单和私有阵容入口，但没有 `admin_notice`；
未知官方私聊身份只获得账号默认的帮助/关于能力，不会获得榜单。管理通知收件人只包含
可私聊用户端点，不包含群成员端点。审计只输出逻辑别名和布尔结果，没有输出 AppID、
OpenID、QQ 号或密钥。该证据证明当前配置按预期编译，不替代真实黑名单消息和管理异常
不外泄测试，因此“权限、故障及实际配置功能”里程碑仍保持待完成。

10:59 用户在当前 `776406ce` 进程的官方群执行“大师段位榜”，客户端收到当前赛季
前 10 名文本，证明大师榜目录、官方群路由、赛季子键、在线榜页和被动回复链真实可用。
随后“米米号”首屏与数字栏目 `2` 均正常路由，但个人巅峰封包的竞技、狂野、专家数据
全部超时，大师栏同时显示“赛季榜未查询”。审查确认两件事：大师没有个人分数来源时
旧实现只查缓存而不在线搜索；且大师榜本来不依赖个人巅峰封包，却被串在封包之后。
提交 `f1ecf6bd` 为大师启用受配置上限约束的无分数在线查找，`fad2d922` 再将大师查询
与个人封包并行，并保持另外三榜只在取得个人分数后做定点搜索。专项 211 项回归通过，
包括“个人封包仍悬挂时大师查询已经启动”的时序测试。当前运行进程尚未加载这两个提交，
因此这里只把大师全服榜记为实机通过；玩家巅峰详情仍等待重启最新版后复验。

同日对当前忽略的开发配置执行一次只输出布尔值、数量和逻辑别名的能力审计。单个 QQ
官方账号已启用，但主动消息与自定义键盘均关闭；账号默认只开放帮助、关于和开发身份
查询。活动服务已启用，配置包含 10 个 B站账号、7 项配置命令和 1 项定时消息。当前环境
没有 AI Key，因此 AI 聊天不能列入这次实机通过或失败；意图动作配置存在也不能替代模型
凭据。当前配置同样没有黑名单测试目标。后续“权限、故障及实际配置功能”验收应先覆盖
已有群策略、管理通知不外泄和已启用推送；AI、黑名单、主动消息及键盘分别等待显式配置
或平台授权，不通过临时硬编码制造测试结果。

## 验收进度

按以下 8 项验收里程碑计数：8/8 有完整完成记录。
这是等权的验收项完成率，不是代码完成率、剩余工时估计或最新版回归通过率。
前四项保留历史实机证据，第五项已由当前精确 digest 的部署回归确认。第六项主要查询已在
`c328a103` 补齐阵容、收集、巅峰、群星牌、三种巅峰池和 owner 幸运橱窗，其中幸运橱窗
连续两次完成确认、无头登录及图片送达；此前同一运行树已复核其余查询。多轮交互和
权限/故障与多轮交互两组均已关闭。自定义键盘未启用，按当前部署能力记为不适用并保留
数字选择路径。进度来自验收证据，不因增加提交数提高。

| 项目 | 状态 | 证据或完成要求 |
| --- | --- | --- |
| SDK 登录、READY、自动令牌更新 | 历史实机通过 | protocol-baseline 中的登录与令牌轮换记录 |
| 官方私聊及群 @ 基础命令 | 历史实机通过 | 私聊帮助、群 @ 命令各返回一次的记录 |
| 图片上传、显示和连续回复 | 历史实机通过 | 群和私聊图片显示确认，连续序号记录 |
| 重连 Resume、正常关闭 | 历史实机通过 | 隔离连接重连和正常关闭记录；腾讯重复重投仍未观察到 |
| 当前生产版本识别 | 已通过 | `ae186314` 精确 digest 已部署；五个 QQ Official 账号 READY、OneBot 静默观察和三个无头账号均通过；`c328a103` 保留为早期完整功能验收基线 |
| 主要查询功能 | 已通过 | A3-A7、A9 已在 `c328a103` 复核；A1、A2、A8、A10、A11、A12 保留前一 `e6b8bdad` 的真实 C2C 回执和 NapCat 客户端证据；A7 由 owner 连续两次确认登录并取得客户端图片 |
| 多轮交互和用户可见行为 | 已通过 | `c328a103` 已确认同一阵容请求的两段文本均合并提及与正文、图片无提及占位；前一 `e6b8bdad` 已完成群帮助数字选择和 C2C 立绘选择；用户已在 `ae186314` 上完成手机 QQ 最终视觉确认 |
| 权限、故障及实际配置功能 | 已通过（含外部 TODO） | 黑名单、功能开关、管理异常不外泄、AI 主提供商、真实备用切换、推送/退订和官方优先均已取得真实证据；腾讯自然产生的拒收/额度事件保留为非阻塞外部 TODO |

## 完成前执行清单（已关闭）

1. 生产已固定到 `ae186314` 精确 digest；不依赖可移动的 `latest` 名称，也不再使用私有
   Preview 镜像。
2. NapCat 辅助账号纯 `@官方机器人 @目标成员`、带目标的米米号查询、真实群成员空
   `@机器人` 与 `@机器人 + 已认领命令` 均已完成对应身份和路由验收。
3. 手机 QQ 多轮视觉确认已完成。C4、C6、A7 和 C7 均已闭环。C8 等待腾讯
   自然产生的 receive/reject 或额度拒绝事件，作为外部 TODO 记录，不再阻塞当前可执行
   验收。
4. 发现阻碍使用的问题时先记录失败证据，再修复、重建并重测所有受影响行。
5. 未完成项保留明确状态，不以 mock、测试数量、命令目录覆盖率或服务端 handler 返回
   推定客户端已经收到结果。

第二 AppID 与多账号实机隔离已经通过。自定义键盘未获授权，按部署能力记为不适用；腾讯
自然拒收或额度事件仍按 External TODO 处理。任何后续验收仍不得伪报通过或暴露凭据。

## 2026-09-20 最新本地门禁

- 最新完成全量验证并部署的代码候选为 `851bf5a9`，全量：`3843 passed, 7 skipped`；跳过项没有计入
  通过。现有警告主要来自 NoneBot `ForwardRef` 弃用提示和两条既有 SQLAlchemy 关系警告。
- 核心/完整 NoneBot 清单启动、OneBot 子进程、QQ Official 生命周期、两套状态迁移和
  只读配置校验专项：`111 passed`。
- 权限、官方投递、生命周期、会话、提示预处理、玩家 portable 命令、Feature、管理通知、
  AI 平台行为、主动投递和退订专项：`320 passed`。
- 另补一条 QQ 官方完整路由测试，证明同一未开放功能的群中超级管理员可绕过、普通成员
  不认领且不转交 AI；QQ 官方 MVP 文件现为 `66 passed`。AI、B站、无头、渲染崩溃和
  红包管理通知专项为 `53 passed`，没有发现绕过 `AdminNoticeService` 向当前普通群发送
  内部异常的路径。两组结果均为代码门禁，不替代 C1/C2/C4 的真实客户端验收。
- Ruff check、全仓 Ruff format check（`858 files already formatted`）、生产与测试
  BasedPyright、compileall、静态仓库检查和 diff check 全部通过。
- `uv.lock` 经 `uv export --frozen --all-extras --no-dev` 生成临时固定依赖清单后，
  `pip-audit` 在关闭 spinner、15 秒网络超时和 120 秒进程总上限下报告无已知漏洞。
- AI provider 配置迁移已进入公开发布：删除单一 `AI_KEY` 及顶层
  `ai.base_url/model/fallback_models/thinking`，改为 `[ai.providers.<alias>]`、
  `ai.provider_order` 与每个 provider 独立的 `AI_KEY_<ALIAS>`。绑定玩家橱窗修复、
  多账号身份观察隔离、Docker 旧镜像回收及后续路由/菜单修复均已进入公开主线。
- 新增 `python -m ironsbot.config_check` 作为只读发布前检查。它复用正式严格加载逻辑，
  但不启动适配器、调度器、数据库或网络客户端；输出只含平台选择和别名摘要。生产迁移
  仍必须先在配置副本上执行，不能把该工具当成自动改写器。

## 剩余实机验收执行矩阵

以下矩阵只用于关闭第 6 至 8 项里程碑。代码测试是部署前门禁，不是实机验收证据。
所有行必须使用同一个精确镜像 digest；若修复后 digest 改变，受影响行必须在新 digest
上重测。每条记录必须包含：输入与上下文、预期、客户端实际显示、时间、脱敏账号指纹、
精确镜像 digest、平台回执或脱敏日志引用、结论。禁止记录原始 AppID、OpenID、QQ 号、
Secret、Token 或 AI Key。

新增实机记录时先用 `scripts/record_official_acceptance.py` 生成标准 JSON。脚本只校验调用者
显式提供的脱敏事实并输出到标准输出，不连接腾讯、不读取生产配置，也不自行写证据文件。
`pass` 必须同时声明客户端可见观察和平台回执或脱敏日志证据；完整 revision、精确镜像
digest、带时区时间及验收行编号均为必填。示例中的值必须使用实际部署候选和脱敏指纹：

```text
uv run python scripts/record_official_acceptance.py \
  --row A7 \
  --revision <40位公开提交> \
  --image-digest sha256:<64位精确摘要> \
  --observed-at 2026-09-20T12:30:00+08:00 \
  --account-fingerprint public_bot:<脱敏摘要> \
  --context "受控 C2C 查询幸运橱窗" \
  --expected "确认菜单和最终图片可见" \
  --actual "客户端显示一次菜单和一张图片" \
  --client-observed \
  --platform-evidence-kind accepted \
  --platform-evidence "腾讯回执分类为成功，原始标识已脱敏" \
  --conclusion pass
```

### 发布与配置前置条件

- 当前生产修订为 `851bf5a9`，manifest digest 为
  `sha256:08978df9a6c19989c3d2e1f8cc05fb1941019c92498b9f875d6cde7b02c67188`。
- CI run `35494510291` 的依赖审计、离线 smoke、体积门禁及 Docker Hub/GHCR 发布均通过；
  Unraid 展开镜像大小为 `260023556` bytes。
- `docs/` 不进入 Docker 构建上下文；本 Spec 的纯文档提交即使生成新的 OCI revision digest，
  也不改变运行时文件。实机验收固定实际部署的精确 digest，不为记录该 digest 循环重建镜像。
- 生产 TOML 已删除顶层 `ai.base_url`、`ai.model`、`ai.fallback_models`、`ai.thinking`，
  并迁移为 `ai.provider_order = ["deepseek"]` 与 `[ai.providers.deepseek]`。原主模型与
  后备模型顺序保持不变；只读 `config_check` 已在候选镜像和运行容器内各通过一次。
- `AI_KEY_DEEPSEEK` 继续由 Unraid 加密环境变量注入。尚未提供 endpoint/model 定义的
  `FUMIN`、`NOMISS` 值已从当前模板清空，原模板只存在于权限为 `600` 的部署备份中；
  在 TOML 正式声明对应 provider 前不得重新填入。
- 当前发布没有运行状态数据库 schema 变化，不需要再次执行平台身份迁移。
- 实机验收从此处开始必须绑定当前 digest；上一 digest 的行为只作历史线索。
- 2026-09-19 的本机源码候选使用第二个公众账号完成了独立 C2C 链路检查：一次
  WebSocket 意外关闭后约 3 秒恢复 `ready`，随后真实“帮助”消息被识别一次，腾讯接受
  被动文本回复序号 1，用户随后确认客户端已经显示首级帮助。第二次隔离运行中，用户依次
  发送 `帮助`、`1`、`2`、`0`、`3`；前四条被当前菜单认领并各获得一次腾讯成功回执，
  `0` 后的 `3` 明确为 `recognized=False`，未被已经退出的菜单消费。该记录不绑定发布镜像
  digest；当时只有 C2C 帮助和退出语义证据，尚未把 A1 整行标记为通过；下方较新的
  生产群聊记录补齐了该缺口。
- 2026-09-20 00:17（Asia/Shanghai），受控 NapCat 测试账号在已授权官方群直接发送
  未带 `@` 的“帮助”。运行于镜像 revision `812d8bc2`、manifest digest
  `sha256:bd09d556838daa7fcae4c7cd24aa74e057c807d497c83483b367b819cb999bc3`
  的官方机器人约两秒后只回复一次，客户端显示按当前身份权限过滤的首级帮助。
  NapCat 历史记录显示该回复由原生 `reply` 与 `text` 消息段组成，确实关联触发消息，
  没有用成员 mention 冒充引用。该证据补齐 A1 的群聊路径并关闭 B8；它不证明
  NapCat 对其它账号发来的入站指令始终静默，因此不用于关闭 C9。
- 2026-09-20 01:08 至 01:28（Asia/Shanghai），继续使用同一 revision `812d8bc2`、
  manifest digest `sha256:bd09d556838daa7fcae4c7cd24aa74e057c807d497c83483b367b819cb999bc3`
  在隔离测试群执行主要查询矩阵。受控 NapCat 账号只负责注入测试输入和读取群历史；
  `local_bot` 负责全部业务回复。玩家首屏约 1 秒送达；收集在进度回复后约 34 秒返回完整
  汇总，并把一个变化中的榜单明确标成失败；巅峰在约 92 秒后返回竞技、狂野、专家的当前、
  历史、场次和胜率，大师及赛季榜超时均明确列出；群星牌约 17 秒返回分数、全服名次和
  样本百分位。玩家菜单选择阵容后，客户端历史依次出现进度、说明和独立图片。普通竞技
  段位榜与大师段位榜均返回指定名次、玩家、分数及更新时间。竞技池、大师池、专家池各
  出现一条带原生引用的图片消息。精灵与立绘连续候选只消费最近会话，随后唯一精灵 ID
  仍直接返回详情渲染图；唯一刻印 ID 直接返回图片和说明，歧义刻印候选选择后也返回图片
  和说明；新增内容菜单及数字选择分别返回图片。群历史与服务端回执一致，OneBot 观察路径
  未产生重复业务回复。幸运橱窗仍必须等待本地 `b94d144c` 修复进入新候选后复验，因此
  本轮不关闭整个主要查询里程碑。巅峰查询本次使用 111 个封包并耗时约 92 秒，记为后续
  性能风险，但结果没有因单项超时丢失其他模式。
- 同一生产 digest 上继续发送“帮助”，待首级菜单送达后发送 `3`。服务端分别只认领一次
  输入并各产生一次 QQ 官方回复；NapCat 群历史显示第二条回复为“赛尔号查询”二级帮助，
  未出现 OneBot 重复回复，因此 B1 已完成客户端可见验收。群聊验收允许由当前已连接的
  `local_bot`（无极圣武）承担，不要求同一逻辑群同时由两个官方账号回复；C2C 验收指定
  使用霍光对应的 `public_bot`。当前生产容器尚未注入该账号的完整凭据，所以霍光私聊项
  保留为部署门槛，不使用 `local_bot` 的私聊结果替代。
- 2026-09-20 03:08，NapCat 测试账号向指定的 `public_bot` 私聊发送“帮助”，QQ 返回有效
  消息 ID，但 45 秒观察窗口内没有收到回复；只读核对确认生产 TOML 与环境均只启用了
  `local_bot`。随后以当前源码和临时严格配置仅启动 `public_bot`，配置检查确认唯一出站平台为
  QQ 官方、OneBot 已关闭；该账号在 03:13:09 进入 READY。03:14:18 再次私聊“帮助”后，
  运行时只认领一次 `C2C_MESSAGE_CREATE`，腾讯接受 `sequence=1` 的被动文本回复，QQ 私聊历史
  在一秒后显示由 `public_bot` 返回的完整帮助菜单。临时实例随后完成有序关闭且没有遗留监听。
  这证明霍光 C2C 及账号专属回复路由在当前候选上可用；生产若要常驻霍光仍需声明并注入
  `public_bot`，而群聊继续只由 `local_bot` 验收，不要求两个账号重复覆盖同一个群。
- 同一 digest 上完成其余无状态多轮交互：帮助菜单发送 `0` 后只回复一次“已退出帮助”，
  随后的 `3` 为 `recognized=False`；帮助菜单建立约 138 秒后发送 `3`，只返回一次明确的
  会话超时提示，没有执行旧选择；引用普通测试文本并输入“关于”时，入站被标识为
  `input_kind=reply`，只执行当前新输入并返回一次项目介绍；玩家资料请求发出约 0.15 秒后
  立即发送阵容选项 `5`，该输入在玩家首屏回复前已被认领，随后只执行一次阵容查询并
  依次送达进度、说明和图片。生产 TOML 未启用 `custom_keyboards`，模型默认值为 `false`，
  因此 B9 记录为 `not enabled`；数字选择路径已经验证。绑定确认会修改真实状态，尚未在
  生产数据上执行。
- 同一 digest 上由受控账号在授权群明确 `@` 当前官方机器人并发送未被命令目录认领的
  短文本。入站被标识为 `input_kind=bot_mention`，AI 主提供商返回预期短句，腾讯只接受
  一次被动文本回复，OneBot 仅观察该官方回复且未发送业务消息，因此 C5 已通过。当前
  `admin` 群策略本身为 `all`，受控 NapCat 账号也不是 `owner`，不能用该群取得 C1/C2
  的真实差异证据；生产官方账号的主动消息已启用，C8 仍需真实额度、拒收与一次送达验收。
- 当前生产 AI 只声明 `deepseek` 一个 provider，`provider_order` 中没有备用项，因此 C6
  不具备真实切换前提。验收前必须另行配置一个有效 backup provider；不能通过让唯一
  provider 失败并返回错误来冒充备用切换。当前官方账号也只有 `local_bot`，容器环境只
  注入该账号对应的 AppID/Secret 变量；霍光 `public_bot` 已通过临时单账号 C2C 验收，但
  若要作为生产常驻私聊账号，仍需完成生产配置与部署。
- 随后确认同一受控账号在限制群 `qingdao` 中是普通成员；该群未开放
  `team_resource_subscription`。该成员发送“战队订阅”后，45 秒观察窗口内客户端历史只含
  原始成员消息，`local_bot` 回复数为 0，也没有执行订阅写入；紧接着同一成员在同一群
  发送允许的“帮助”，2 秒后收到 `local_bot` 的一次原生引用回复。阳性对照排除了群消息
  未到达或机器人离线，因此 C2 已通过。C1 必须由 `owner` 在同一群发送同一命令，不能让
  辅助账号冒充超级管理员，仍待真实操作。
- 2026-09-20 08:44，`owner` 在同一限制群直接发送“战队订阅”，`local_bot` 返回“本群还
  没有订阅战队”及管理员操作说明；同一 Feature 对普通成员保持上述静默，因此 C1 的
  超级管理员绕过已通过。紧邻的“@机器人 + 战队订阅”虽然被运行时识别为
  `input_kind=bot_mention`，却错误进入 AI：全量群事件的结构化自身提及没有稳定提供
  `username`，旧归一化依赖该字段，正文中的开头提及未被移除，命令目录因而无法认领。
  提交 `db585f5c` 改为仅在 `mentions[].is_you=true` 已确认自身提及时移除正文开头的一个
  提及段，不依赖显示名，并保留后续成员提及；137 项官方运行时、命令优先、AI 路由及
  订阅所有权回归通过。2026-09-20 随后在最终公开镜像提交 `1f56bcc1`、digest
  `sha256:d865ec1f67c603f1b1ee39d412c4afbd0a8402c30de4bc5688dda948e15b0994` 上，受控
  NapCat 账号和真实客户端分别发送结构化“@机器人 + 战队订阅”。机器人只返回 AI 的
  “不支持战队订阅”回答，没有执行订阅查询。首次自动判据因 AI 文案也包含“订阅战队”而
  误报通过，现已作废。AI memory 的脱敏结构检查证明实际 prompt 以 36 字符的 `<@!...>`
  标记开头；生产命令目录离线重放则能正确认领纯“战队订阅”，故根因是官方入站正文只
  清理 `@名称`、没有清理腾讯结构化自身提及标记，而不是 Feature、权限或命令契约缺失。
- 2026-09-20 使用霍光和隔离 TOML 完成 C3：同一受控逻辑身份同时声明 OneBot QQ 与
  `public_bot` C2C 端点，加入 `bot.superusers`，保持 `superuser_bypass=true`，并在用户策略中
  显式启用 `blacklist`。同一身份未加入黑名单时发送“帮助”会在一秒内收到菜单；加入后再次
  发送，官方入站事件到达但未被命令路由认领，45 秒客户端观察窗口内只有用户原消息，没有
  腾讯发送回执或机器人回复。临时配置和状态目录随后删除，因此 C3 已通过且未改生产策略。
- 2026-09-20 使用霍光和隔离 TOML 验证 C4 的私聊管理员路径：配置只声明 `public_bot`，将
  受控 C2C 身份设为唯一超级管理员，不声明任何群身份或群策略，并将唯一 AI provider 指向
  本机固定返回 HTTP 501 的测试端点。受控私聊发送唯一标记文本后，AI 请求只执行一次；腾讯
  随后接受一条主动管理员异常通知和一条被动通用失败回复，NapCat 私聊历史也恰好显示这两条
  机器人消息。不可用的 OneBot 私聊端点被明确跳过，没有产生 OneBot 发送。该证据只完成
  C4 的超级管理员私聊通知链，不替代普通群“零异常消息”的真实负向观察；群聊验收继续只由
  `local_bot`（无极圣武）承担，C2C 验收使用 `public_bot`（霍光），同一管理群无需重复配置
  两个官方账号。
- 当前容器启动日志还记录了四段 QQ 官方 C2C 主动通知获得平台成功回执，同时对应 OneBot
  私聊目标均以 unsupported conversation 明确跳过。这为 C8/C9 提供服务端侧部分证据，但
  尚无本轮客户端显示确认，也未受控制造官方发送失败，因此不用于宣告主动消息完整通过或
  “失败不回退”已经实测。随后只读检查 `admin` 群最近 500 条历史；没有找到可归为启动通知
  或业务主动推送的官方群消息，非引用项均为被动回复的图片/文本分段，因此 C8 继续待验收。
- 精确源码提交 `c6f3bada` 修复了官方单平台从不触发 `first_bot_connect`、因而无法发送启动
  通知的问题：只有 QQ Official 被选为出站平台时，通知会在账号 READY 后通过通用 startup
  hook 尝试；OneBot 单平台继续等待首次连接，双平台后续连接复用 `sent/sending` 状态而不会
  重发。霍光的隔离官方单账号实例取得 proactive 成功回执，NapCat 客户端历史中专用验收
  文案恰好出现一次，短观察窗内未重复；同一逻辑身份的 OneBot 私聊端点被明确跳过。实例、
  监听、临时身份配置和状态目录均已清理。该证据完成一次送达，不覆盖平台拒收和额度错误，
  因此 C8 仍保留平台拒收和额度错误两项剩余门槛。
- 同一候选随后以完整 `public_bot` 凭据、`bot.onebot.enabled=true`、
  `bot.onebot.send_messages=true` 和不存在的隔离官方目标执行失败路由验收。配置预检仍确定
  唯一出站平台为 QQ Official、OneBot outbound 为关闭；启动通知的 OneBot 端点以
  unsupported conversation 跳过，腾讯真实接口随后返回 `11255/permanent`，没有出现 OneBot
  重试或备用送达。结合生产中 NapCat 与 `local_bot` 同时在线时全部业务回复仅由官方端送达，
  以及 transport 门禁对官方失败后 OneBot messenger 零调用的断言，C9 已完成。隔离实例和
  状态目录均已清理，失败目标不是任何真实用户。
- 2026-09-20 在同一生产 digest 上使用已授权的 NapCat 群管理员账号执行战队资源订阅
  实机链路：先确认当前群无订阅，再订阅一个真实战队，随后通过“战队订阅”和“战队”
  分别读取持久状态及实时资源，最后取消订阅并再次确认当前群无订阅。每一步客户端均只
  收到 `local_bot` 的一次原生引用回复，OneBot 没有业务回复；测试结束后生产订阅状态恢复
  原状。该证据证明订阅、读取、取消和官方送达，不证明定时任务实际触发、容器重启后的
  持久化，或退订后下一次调度被抑制，因此 C7 仍只记部分通过。当前生产扫描时间为每天
  `23:00`，没有正式的管理员立即调度入口；直接调用内部 `scan()` 或临时修改生产时间均不
  作为真实调度证据。
- 2026-09-20 随后使用霍光、当前源码和隔离 TOML 完成 C7 剩余闭环。第一次启动只声明
  `public_bot`，注册一条带秒级执行时间和唯一文案的私聊定时消息；腾讯接受一次主动文本，
  NapCat 私聊历史中该文案恰好出现一次。受控用户随后打开 TD 菜单并选择该定时项，客户端
  收到退订确认，隔离 `qq_state.sqlite` 写入唯一的 QQ Official 私聊退订记录。进程正常关闭后，
  保留同一状态库、同一稳定 schedule id，改用第二条唯一文案和新的执行时间重新启动；启动
  清理保留该退订记录，调度按时运行后第二条文案在客户端历史中为 0 条。两次实例均正常关闭，
  临时配置未修改生产数据。结合上一条生产战队订阅 CRUD 证据，C7 在当前源码上已通过；最终
  发布镜像仍须按统一 digest 复验受后续提交影响的路径。
- 2026-09-20 在生产精确镜像 `667177ea`、Docker Hub digest
  `sha256:bae99232f7c816349f9e1505bbc27579591ba3ef76963f2426d2eaf3cf3dec15` 上完成
  C7 最终复验。临时唯一 schedule 首次在受控管理群恰好送达一次；管理员通过真实 TD 菜单
  退订后，`push_unsubscriptions` 产生唯一 QQ Official group 作用域记录。容器重启后该记录
  仍为一条，第二个执行时间到达后客户端历史中第二条唯一文案为零。随后通过订阅菜单恢复，
  记录归零；原生产 TOML 已从操作前备份恢复，临时 feature/schedule 引用为零，状态库
  `integrity_check=ok`，容器正常启动并重连 OneBot。C7 至此在最终生产 runtime tree 上通过。
- 本地 `b94d144c` 幸运橱窗绑定解析修复在当前 HEAD 上完成专项回归：幸运橱窗、玩家命令
  与 QQ 官方运行时/MVP 共 `252 passed`，Ruff、BasedPyright、compileall 和差异检查通过。
  本机 Docker Linux engine 当时未运行，因此没有生成新的精确镜像；A7 仍必须在包含该修复
  的发布 digest 上取得客户端结果后才能通过。
- B3 探测期间曾误用立即生效的“绑定米米号”命令，而不是玩家详情中的确认选项。受控
  测试身份因此短暂写入一个绑定；核对 2026-09-16 迁移备份后确认其此前一直处于未绑定、
  已完成选择状态。随后先用 SQLite backup API 建立独立备份，再以主键、玩家和更新时间
  三重守卫恢复唯一一行，最终 `integrity_check=ok` 且字段与历史前态一致。恢复后由于
  `choice_completed=1`，详情不会再次提供确认选项；后续 `n` 未被认领，所以 B3 仍未通过。
- 2026-09-20 使用 `public_bot` 和完全隔离的临时数据目录重新建立真正的新身份；启动前配置
  检查确认只启用 QQ 官方出站和 `seer_player`，账号进入 READY。测试账号私聊发送明确玩家
  查询后，命令被认领一次且错误回复在客户端可见，但赛尔号游戏服务器当时维护或未开放，
  因而无法生成玩家详情与绑定确认菜单。关闭后隔离状态库中 `player_bindings` 行数和已完成
  选择数均为零，失败没有消耗该身份；临时实例、监听和数据目录均已清理。B3 仍待服务器
  开放后按相同流程完成一次确认和一次取消，不能用立即绑定命令替代。
- 03:51 再次按相同的 `public_bot` C2C 隔离流程探测，腾讯接受一次被动回复，客户端仍收到
  “服务器维护或未开放”的明确错误；临时库仍为 `player_bindings=0`，进程、监听和目录均已
  清理。当前源码随后删除了无调用方的旧绑定提示构造路径，玩家详情首次查询只引导
  `y/n` 或菜单中的“设为默认米米号 / 暂不绑定”，不再引导立即生效的绑定命令；专项
  `305 passed`，但这项代码验证不替代服务器开放后的真实确认与取消验收。
- 04:20 至 04:35，服务器恢复后在当前源码候选上使用霍光对应的
  `public_bot` 完成两次完全隔离的 C2C 验收。两个实例均只启用 QQ Official 出站，
  且无头客户端成功进入游戏服务器。NapCat 私聊历史记录的第一条序列为“明确玩家
  查询 → 玩家详情及绑定提示 → `n` → 无绑定提示的玩家详情”；该独立状态库恰好
  一条记录，`choice_completed=1` 且未保存米米号。第二条序列将 `n` 换为 `y`，其独立
  状态库同样恰好一条记录，`choice_completed=1` 且保存的米米号与查询目标一致。四次
  腾讯被动文本回复均有成功回执，输入每次只被路由认领一次，因此 B3 已完成。
- 05:00 前后在提交 `967ab543`、`qqbot-agent-sdk==1.2.2` 的当前源码候选上继续使用霍光
  `public_bot` 完成 A7。发现实例只启用“官方身份”，程序从真实 C2C 回复中取得账号作用域
  标识并只写入隔离配置；随后临时身份通过正式规范化链映射到隔离 OneBot 身份，并在独立
  状态库绑定配置的开发米米号。首次查询用于识别测试配置中同一账号兼作后台 worker 会触发
  一次挤线，因此不作为最终证据；干净复验关闭后台 worker、清空隔离的当天缓存后重新发送
  “橱窗”并选择确认。专用会话独立完成登录，缓存恰好写入一行四个皮肤，腾讯接受一条确认
  菜单文本和一条最终图片，NapCat 私聊历史也恰好显示这两条机器人消息及一个图片段，没有
  OneBot 业务回复或连接异常。实例随后有序关闭。该证据完成当前源码候选的 A7，但仍须在
  最终发布镜像的精确 digest 上复验后才能关闭“主要查询功能”里程碑。
- 同日本机源码候选还使用完整功能清单完成了隔离生命周期检查：28 个插件贡献和 89 个
  QQ 官方直接命令完成装载，公众账号保持 `ready`，OneBot 消息处理保持关闭；一个配置的
  无头 worker 完成认证并进入游戏服务器。官方连接和无头客户端随后均正常关闭。这证明
  公众配置能够组成主要查询依赖，但不代替 A2-A12 的客户端结果与平台送达证据。
- 2026-09-20 完成三仓发布契约复核：seerapi 本地 `main` 纯快进到远端 `0c7c9d5`，全量
  351 passed、Ruff 通过；当前 `seerapi-data-latest` 发布物
  为 133435392 bytes，下载 SHA256 与发布校验文件一致，并通过本仓库 schema contract v2
  消费者校验。private 保持 `9c3d61e` 与远端一致，显式对接当前公共源码后 27 passed，Ruff
  和 BasedPyright 通过；原有未跟踪 `uv.lock` 未被修改或提交。该结果证明当前生产者、
  消费者和私有扩展接口兼容，不代替任何真实 QQ 客户端验收行。

### A. 主要查询功能

| 验收行 | 输入与上下文 | 客户端完成条件 | 状态 |
| --- | --- | --- | --- |
| A1 帮助 | 群聊与 C2C 分别发送帮助 | 首级帮助可见且只显示当前身份可用项 | 已通过；C2C 与官方群未带 `@` 路径均有客户端/平台证据 |
| A2 玩家信息 | 已绑定身份查询米米号 | 玩家首屏送达，资料与绑定对象一致 | 已通过；当前生产 digest 的玩家首屏与明确查询对象一致 |
| A3 收集 | 从玩家菜单进入收集 | 完整文本或图片结果送达，失败时显示明确原因 | 已通过；完整汇总送达，单项榜单变化有明确失败语义 |
| A4 巅峰 | 从玩家菜单进入巅峰 | 竞技、狂野、专家和大师状态均有明确结果，不因单项超时丢失其他项 | 已通过；四种模式均有结果或明确超时，其他模式未丢失 |
| A5 群星牌 | 从玩家菜单进入群星牌 | 完整结果送达，缺失数据有明确语义 | 已通过；分数、全服名次和样本百分位可见 |
| A6 阵容 | 已绑定身份发送阵容 | 进度、说明和阵容图片均在客户端可见 | 已通过；当前生产 digest 的三段客户端消息均可见 |
| A7 幸运橱窗 | 已绑定身份发送橱窗 | 使用当前绑定米米号；超级管理员可查询已配置登录账号，普通用户仍须通过账号所有权校验 | 已通过；`c328a103` 中 owner 连续两次进入 `1/2/0` 确认会话，回复 `1` 后无头登录成功并由腾讯回执及 NapCat 客户端确认 PNG 送达；`667177ea` 的视觉复验已确认中文、四张立绘、风尚券和钻石图标正常 |
| A8 全服榜 | 分别查询至少一个普通榜和大师段位榜 | 名次、玩家、分数及更新时间可见 | 已通过；当前生产 digest 的竞技榜和大师榜均返回指定名次 |
| A9 巅峰池 | 分别发送竞技池、大师池、专家池 | 三项均显示对应渲染图，不退化成无说明的空回复 | 已通过；三项客户端历史均包含原生引用与图片 |
| A10 精灵与立绘 | 查询一个精灵及其立绘 | 详情与图片均送达，候选选择不串会话 | 已通过；立绘候选和唯一精灵详情均送达且未串会话 |
| A11 刻印 | 触发唯一结果和歧义候选各一次 | 唯一结果直接送达；候选可选择并显示图片与说明 | 已通过；唯一与歧义选择路径均显示图片和说明 |
| A12 新增内容 | 发送新增内容并选择一个栏目 | 菜单、数字选择和最终图片均可见 | 已通过；修复后的菜单图片与数字选择结果图片均可见 |

### B. 多轮交互与用户可见行为

| 验收行 | 操作 | 客户端完成条件 | 状态 |
| --- | --- | --- | --- |
| B1 二级帮助 | 帮助后发送有效序号 | 只返回一次对应二级帮助 | 已通过；当前生产 digest 的群历史只出现一次“赛尔号查询”二级帮助，OneBot 未重复回复 |
| B2 候选选择 | 触发歧义查询后发送序号 | 选择只作用于发起者和当前会话 | 已通过；同 digest 的连续精灵/立绘及刻印候选均选择正确会话 |
| B3 绑定确认 | 使用尚未完成绑定选择的身份查询明确玩家，再在详情菜单使用 `y`/`n` 或平台已授权按钮；不要改用立即生效的“绑定米米号”命令 | 确认与取消各自只执行一次，绑定结果可查询 | 已通过；霍光 C2C 的两个独立状态库分别证明 `n` 不保存米米号、`y` 保存查询目标，客户端历史与腾讯回执均只有一次对应回复 |
| B4 退出 | 菜单中发送 `0` | 会话结束，后续数字不再被旧菜单消费 | 已通过；当前生产 digest 返回退出提示，后续数字未认领 |
| B5 超时 | 建立菜单后等待超过 120 秒再回复 | 返回明确过期反馈且不执行旧选择 | 已通过；约 138 秒后的选择只返回明确超时提示 |
| B6 加载期快速回复 | 长查询仍在加载时立即发送有效选择 | 输入被保留并在菜单就绪后执行一次 | 已通过；首屏送达前的阵容选择被保留并只执行一次 |
| B7 引用输入 | 引用普通消息并输入新命令 | 只解析当前新输入；引用元数据保留且不污染命令 | 已通过；同 digest 只执行引用消息中的新“关于”指令 |
| B8 被动回复身份 | 检查群回复的协议关联 | 使用入站 `msg_id` 和连续 `msg_seq` 保持被动回复；不得把主动消息伪装成回复 | 已通过；首条含成员提及的 Markdown 使用被动 `msg_id`/`msg_seq`，但不附加会导致客户端重复正文的 native source reference |
| B9 自定义按钮 | 仅在账号确有平台授权且配置启用时操作按钮 | 按钮值与数字选择等价；未授权时记录 `not enabled`，数字路径必须可用 | `not enabled`；生产配置使用默认 `custom_keyboards=false`，数字路径已通过 |
| B10 发令成员提及 | 在全量群消息模式直接发送命令，再检查官方回复 | 每个请求的每条文本回复都以“发令成员提及 + 换行 + 正文”组成一条被动回复；纯图片不得产生单独 `@` 消息、重复正文或字面协议标签 | 已通过；`c328a103` 的阵容链路证明所有文本段同条携带原生提及、换行且图片无提及占位，`ae186314` 上用户进一步用手机 QQ 确认提及与正文同条、换行正确、正文只出现一次 |
| B11 `@` 命令优先 | 发送 `@机器人 + 有效命令` | 业务命令执行一次，AI 不调用 | 已通过；`e32b2806` 精确 digest 返回一次订阅查询结果，AI memory 无新增 prompt |

### C. 权限、故障与实际配置

| 验收行 | 场景 | 完成条件 | 状态 |
| --- | --- | --- | --- |
| C1 超级管理员绕过 | 在未开放目标 Feature 的群由超级管理员执行命令 | `superuser_bypass=true` 时成功执行 | 已通过；`owner` 在限制群直接执行未开放的“战队订阅”并收到真实业务结果，普通成员同群同命令保持静默 |
| C2 普通成员隔离 | 同群同命令由普通成员执行 | 不执行业务；响应符合既有静默/无权限策略 | 已通过；限制群普通成员发送未开放的战队订阅命令后保持静默，45 秒内无官方回复且无业务写入；同群帮助阳性对照正常回复 |
| C3 黑名单优先 | 将受控测试用户或群加入黑名单后再由超级管理员触发 | 黑名单优先于超级管理员绕过，业务不执行 | 已通过；隔离配置中的跨平台超级管理员同时加入黑名单后，官方入站到达但 45 秒内无业务执行或回复 |
| C4 管理异常隔离 | 触发受控 AI 或后台失败 | 只通知超级管理员和 `admin_notice` 管理群，普通群无泄露 | 通过；隔离 AI 失败验证管理员 C2C 通知链，最新生产 digest 的真实无头登录失败及恢复通知选择 2 个私聊目标、0 个群目标，腾讯只接受 1 条 C2C 主动通知；同一 85 秒窗口内 NapCat 观察到 38 条群消息，群主动回执和管理异常关键词消息均为 0 |
| C5 AI 主提供商 | 配置至少一个真实 provider 后发送未认领输入 | 用户收到一次 AI 回复，日志只含 provider/model 别名 | 已通过；明确 `@` 后的未认领短文本只收到一次预期 AI 回复 |
| C6 AI 备用切换 | 受控令主 provider 失败并保留有效 backup | 自动转到后备 provider；不向普通群播报内部异常 | 已通过；使用 IronsBot 正式 `HttpAiCompletionClient` 令独立主 provider 真实返回 HTTP 401，随后自动切换到 NoMiss `gpt-5.6-luna`，取得 HTTP 200 和唯一 `FAILOVER_OK` 回复；凭据未写入仓库或生产配置，异常隔离由 C4 实机证据及专项门禁覆盖 |
| C7 推送与退订 | 使用当前实际启用的推送执行订阅、送达、退订、再次调度 | 首次一次送达；退订后抑制；状态持久化 | 已通过；`667177ea` 生产精确镜像完成一次送达、真实 TD 退订、容器重启持久化、第二次调度零送达及恢复订阅，临时配置和状态均已清理 |
| C8 主动消息 | 仅在生产确实启用且平台授权时触发 | 验证额度、拒收和一次送达；未启用则记录 `not enabled`，不得伪报通过 | 已完成当前可控范围，外部 TODO；官方单平台 startup 主动私聊已取得一次腾讯回执和一次客户端显示，receive/reject 状态持久化、发送前抑制及额度错误分类已有测试门禁；腾讯真实拒收或额度耗尽事件无法主动制造，发生后补录证据 |
| C9 官方优先与 NapCat 静默 | 同时提供官方凭据并连接 OneBot | 只有官方端发送；NapCat 不回复、不主动发送且失败不回退 | 已通过；生产双端在线时被动业务仅由官方端回复，官方主动 startup 也只送达一次；隔离真实 `11255/permanent` 失败没有触发 OneBot 重试 |

### 代码侧发布门禁

2026-09-19 对权限、官方投递、生命周期、会话、提示预处理、玩家 portable 命令、
Feature、管理通知、AI 平台行为、主动投递和退订执行专项回归，共 `320 passed`。其中已经
覆盖超级管理员绕过、普通成员 Feature 限制、黑名单、管理通知目标、AI 异常隔离、主动
消息禁用/失败、账号级键盘能力、真实 source reference、绑定确认、退出、120 秒会话过期
和加载期快速选择等代码路径。这些结果只证明候选在测试环境符合契约，不能将上表任何
“待验收”行改为通过。

2026-09-20 平台选择、禁用 OneBot outbound、启动装配和平台路由专项为 `33 passed`；
新增断言证明 QQ 官方发送返回 `transport_unavailable` 时，即使路由器同时持有 OneBot
messenger，也不会调用它。该门禁防止跨平台回退回归，但 C9 仍需真实官方失败证据才能
完整通过。

提交 `c6f3bada` 及其前置绑定菜单、配置预检修复完成全量候选回归：`3825 passed, 7 skipped`；
全仓 Ruff、`ironsbot` 与 `tests` 两次 BasedPyright、compileall、静态仓库检查和差异检查均
通过。跳过项不计为通过，代码门禁也不替代上表剩余的真实客户端和平台拒收/额度证据。

2026-09-20 再次审计 `c6df4c3d..3f900b5e`：范围内只修改本验收文档，生产代码、测试、
配置模型、容器和依赖锁均无变化，因此上述全量代码门禁仍适用于当时源码。当时本机
Docker Linux engine 与 WSL 服务不可用；后续已经由公开 workflow 从完成态 HEAD 构建
精确候选，历史 `812d8bc2` 仍只用于解释此前客户端行为。

提交 `db585f5c` 修复全量群事件缺少稳定 `username` 时的自身提及正文归一化，并新增一项
回归；提交 `b4b34a33` 只恢复三个历史文件的仓库格式。随后从该精确 HEAD 运行全量候选
门禁：`3826 passed, 7 skipped`，全仓 Ruff lint 与 858 文件 format check、生产与测试
BasedPyright、compileall、静态仓库检查和 diff check 全部通过。当时新修复仍须构建新 digest
并在客户端复验“@机器人 + 战队订阅”；构建门由公开 `64058429` 候选完成，但
`1f56bcc1` 精确 digest 的真实复验发现腾讯正文使用 `<@!...>`，仍会进入 AI。该失败不能
由此前只覆盖 `@显示名` 的回归测试替代。

提交 `c25b4dc9` 对照原版 OneBot `PromptSession.build_message()`，将 portable 查询候选从
“名称与说明分两行”恢复为同一行的 `名称（说明）`，子候选仍保留 `↳` 层级。因此精灵、
立绘/皮肤和刻印等共享查询统一显示为 `雷伊（70）`、`V8-01（41112 物速）`，没有为各
查询类型增加专用格式分支。相关 115 项及全量 `3826 passed, 7 skipped`；全仓 Ruff
lint/format、生产与测试 BasedPyright、compileall、静态仓库检查及 diff check 全部通过。

公开 workflow `35482126752` 随后从完成态 `64058429` 构建并发布 manifest digest
`sha256:2aba0e2a9d9f2ab818fc4f7c6d9b2299b5f55dbafd49d970cd1982d0ea8a828b`。镜像展开大小
260,019,690 bytes，与上一公开基线增长 0；离线 smoke、依赖审计和体积门禁通过。该证据
关闭“尚未构建最终候选”的发布门，但不替代受影响路径的 Unraid 部署和客户端复验。

随后在当前生产 `1f56bcc1` 精确 digest 上使用受控 NapCat 账号执行“雷伊立绘 → 1”。群历史
只出现一次候选菜单，名称与说明保持 `雷伊（70）` 同行格式，旧的分行编号格式没有出现；
数字选择返回对应结果标题和图片，字面 `<qqbot-at-user>` 为零。另一次菜单用 `0` 退出，未
触发渲染或持久化修改。该证据关闭最终 digest 的候选菜单与数字选择复验。

提交 `e32b2806` 使用 `mentions[].is_you` 和对应 `member_openid` 精确清理腾讯
`<@...>`/`<@!...>` 自身提及标记，同时保留先出现的其他成员提及；已知但未授权的命令也有
回归保证不会交给 AI。全量 `3842 passed, 7 skipped`，Ruff、格式、生产与测试两套
BasedPyright、compileall、静态仓库检查及 diff check 全部通过。公开 workflow
`35491171999` 成功发布 digest
`sha256:d1651f41178cd00eaab070e8ab64c5adce805ff1be405c5e0ff3b4767c46328d`，Unraid 在 root-only
备份后部署该精确镜像；配置严格加载、QQ Official READY、OneBot observer、三无头 worker
及共享 Docker 网络均健康。随后结构化“@机器人 + 战队订阅”只返回一次“本群还没有订阅
战队”的业务结果，AI memory 增量为零，来源引用和发令成员 mention 均存在，字面协议标签
为零。B11 与第六个验收里程碑由此关闭。

同日进一步发现，把成员提及和长正文放在同一自定义 Markdown 中会让 QQ 客户端同时显示
Markdown 正文和平台生成的纯文本回退；移除 `message_reference` 不能消除重复。TEXT
`<@OpenID>` 与 `<qqbot-at-user>` 对照均只显示字面文本，不能产生真实提及。提交
`851bf5a9` 将每个请求收口为“首条最小 Markdown 提及 + 一条 TEXT 正文”，后续阶段不重复
提及，以免超过同一入站消息 5 次回复预算。全量 `3843 passed, 7 skipped` 及全部静态门禁
通过；workflow `35494510291` 发布上述精确 digest 并部署到 Unraid。受控群“关于”命令的
最终历史恰有两条官方回包：第一条包含一个 `reply`、一个 `markdown` 和一个 `at`，第二条
只含一个 `text`；版本正文出现一次，原始 OpenID 标签出现零次。B10 以该最终证据重新关闭。

2026-09-21 根据新的交互要求，正式目标改为一条 Markdown 被动回复：发令成员提及、
换行和正文在同一 payload 内，请求携带原入站 `msg_id` 与一个 `msg_seq`。上述两次回包仍作为
历史对照保留，不再是当前目标行为；B10 在新精确 digest 完成客户端复核前重新打开。

同日按生产安全流程为受控 OneBot 测试身份增加一个非公开游戏账号与幸运橱窗授权，密码
仅通过 Unraid 加密环境变量注入。修改前备份了 TOML、Unraid 模板和容器定义；严格
`config_check` 通过后原样保留端口、挂载、双网络和 Unraid 管理标签重建容器。运行镜像为
纯文档修订 `03b83f3a`，runtime tree 与全量验证的 `851bf5a9` 相同。真实“绑定米米号”回包
确认绑定已落库；随后“橱窗”显示登录确认菜单，数字 `1` 完成隔离登录并送达一张最终图片。
缓存审计确认当天结果恰有四个正数皮肤 ID，容器配置和 Unraid 模板中的密码变量各存在一次，
值未写入 TOML、Git 或验收记录。绑定玩家资料的后台榜单预热有六项超时降级警告，但未影响
玩家主回复、幸运橱窗登录、图片投递或缓存。A7 与“主要查询功能”里程碑由此关闭。

2026-09-20 本地候选 `dd7e3fa2` 当时在公开 `origin/main` 之后包含四个尚未推送的提交：
恢复幸运橱窗最终卡片布局、恢复新增内容根图中的竞技/专家/大师池变化、补录原版修复
回归审计，以及恢复固定口令和会议口令对明确 `@成员` 的定向回复。最后一项同时覆盖
OneBot 与 QQ Official：没有明确目标时仍回复发令者；存在目标时只提及目标；关键词自动
回复不因成员提及而获得命令所有权。全量结果为 `3852 passed, 7 skipped`，Ruff lint/
format、生产与测试 BasedPyright、compileall、静态仓库检查和 diff check 全部通过。

该本地候选尚无容器或客户端证据，不能替代上表 A7、A12、B10、C4、C6、C8 的真实验收。
本机 Docker Desktop 4.90.0 因 `AppData/Local/Docker/run` 中残留的
`sailor-ingest.sock` reparse point 在启动时无法改名而退出；Ubuntu WSL 同时返回
`Wsl/Service/E_UNEXPECTED`，因此无法在本机生成精确镜像。当前执行策略也拒绝删除该
Docker 运行时残留，未改动镜像、卷、WSL 数据盘或生产部署。恢复本机构建引擎或明确
推送后，应从 `dd7e3fa2` 或其后继精确 HEAD 构建，再重测上述受影响行；不得继续引用
`64058429` 的旧 digest 作为这些新修复的证据。

上述候选随后连同正式的 AppID 密钥寻址方案进入公开 `main`。提交 `de4bdbbe` 将官方
AppID 固定在严格 TOML 账号声明中，AppSecret 只接受 `APP_SECRET_<AppID>`；旧的按账号
别名命名的 AppID/Secret 环境变量、TOML `secret` 和未声明 AppID 的 Secret 均直接报错，
没有兼容双读。全量结果为 `3852 passed, 7 skipped`，Ruff lint/format、生产与测试
BasedPyright、compileall、静态仓库检查和 diff check 全部通过。公开 workflow
`35503424404` 成功发布 digest
`sha256:4cd7f3735a0e299f6a207583e332e6b373677449b5c18f279ef0a85e929a7a02`；Unraid 精确部署
`sha-de4bdbb` 后测得展开大小 260,040,445 bytes，`/app`、site-packages 和字体分别为
3,273,349、102,592,867 和 19,882,302 bytes。

生产配置先备份再迁移到三账号声明，Unraid 模板中的三个 AppSecret 及其他凭据项均设置
为掩码显示。严格 `config_check` 确认 QQ Official 是唯一出站平台、OneBot outbound 关闭、
observer 身份验证启用；三个账号均取得 AccessToken 并进入 READY，应用完成启动，公众账号
随后完成一条 C2C 入站识别和被动回复。第三账号是单群专用账号，当前只开放官方身份查询；
目标群已有稳定别名 `youqijiang`，但仍缺该 AppID 作用域下由真实群事件取得的
`group_openid`，因此没有猜测复用数字群号或其他账号的 OpenID，也尚未开放该群完整
Feature。该外部门只影响第三账号目标群策略，不推翻前两个账号和新配置结构的部署证据。

随后公开 workflow `35507207052` 从 `64ff7de7` 成功发布 manifest digest
`sha256:999930e6884b68309e393987d034e86afcf41520d073fa8d246bfd420c51d723`。
该 workflow 上传的镜像证据记录展开大小为 `260050515` bytes（约 `248.00 MiB`），其中
`/app` 为 `4824 KiB`、Python site-packages 为 `106000 KiB`、字体为 `19452 KiB`，均通过
发布工作流预算；这些数值只证明远端 `64ff7de7`，不能外推为本地后继候选的镜像结果。
Unraid 模板已从历史 SHA 固定值恢复为 `murmansk5000/ironsbot:latest`，运行容器则按 OCI
revision 核验为 `64ff7de7f28be85f9efde1b6931067dbbf32aab6`。部署时人工删除了停止的回滚容器、
一次性 Watchtower 容器和不再被容器引用的历史公开 IronsBot 镜像；私有扩展、Watchtower
及其他仓库镜像均未删除。自动清理实现仍是后续本地提交，不能把这次人工清理记作其部署证据。

第三账号的启动权限收口为 `help`、`about` 和 `qq_official_identity_info`，只用于新群首次
交互和身份诊断。真实生产日志随后记录一次脱敏的 `silent group link confirmed`，状态库
确认该账号已有一条群映射且重启后仍存在；这关闭了上文“缺 group_openid”的外部门。
身份发现没有第二套“入站 @”API：纯 `@机器人` 在 AI 关闭时产生统一的限流帮助提示，
启用的普通指令产生正常业务回复；两者都由官方机器人在首条回复中提及发令成员，再由
NapCat 静默观察同一条可信回复，将官方群 OpenID/成员 OpenID 与数字群号/QQ 号配对。
群映射和成员映射均可由一次严格、唯一且一致的可信回复确认；该账号当前成员
映射仍为零，因此成员关联尚未取得实机完成证据。

当前腾讯事件订阅文档（2026-09-11 更新）将 `GROUP_MSG_RECEIVE`、
`GROUP_MSG_REJECT`、`C2C_MSG_RECEIVE`、`C2C_MSG_REJECT` 以及机器人/好友增删事件列在
`GROUP_AND_C2C_EVENT (1 << 25)` 下。SDK 1.2.2 尚未分发这些事件，因此本地后继候选在
既有 SDK 边界统一转发并持久化收件状态。明确拒收或移除后，主动消息会在平台 API 调用前
返回永久失败；重新开启后恢复，未知状态保持现有 TOML 行为，被动回复不受影响。该实现
完成代码侧契约，但生产尚未捕获腾讯真实 receive/reject 事件，也未取得额度拒绝证据，
所以 C8 仍保持“部分通过”。

2026-09-20 对腾讯事件详情页再次进行字段级核验：群接收开关事件体使用
`group_openid`、`op_member_openid` 和 `timestamp`，C2C 接收开关事件体使用 `openid`
和 `timestamp`。本地实现以 `AppID + conversation kind + group_openid/openid` 作为持久化
键，没有把群成员 OpenID 当作 C2C 用户 OpenID。提交 `a9f7db19` 的相关专项与随后完整
候选门禁均通过；这仍是协议和代码证据，不替代腾讯真实事件。

同一轮只读生产核验确认：当前 TOML 仅声明 `deepseek`，该 provider 配置两个顺序模型；
容器只有对应 provider 的密钥非空，其他预留变量为空且未声明为 provider。因此当前部署
具备同 provider 的模型顺序切换，但不具备 C6 要求的跨 provider/key 真实 failover。不得
通过故意使两个模型共用的同一密钥失败，或使用 mock provider，把 C6 改为通过。

以生产与测试代码树 `70b48254` 完成最终本地代码门禁：`3868 passed, 7 skipped`；全仓
Ruff lint、862 文件 format check、生产与测试 BasedPyright、compileall、静态仓库检查和
diff check 全部通过。该候选包含官方收件状态持久化、镜像清理保护和菜单退出项统一布局，
并新增同一 AppID、同一目标字符串在群聊与 C2C 间保持拒收状态隔离的回归证据；该测试
证据不替代腾讯真实 receive/reject 事件。其后的纯文档提交另行通过静态仓库与 diff 检查，
不改变上述生产与测试代码树。候选尚未推送、构建或部署，生产仍运行公开
revision `64ff7de7`。本机 Docker Desktop 4.90.0
在 Windows build 26200 上反复创建不可访问的 `sailor-ingest.sock` AF_UNIX 重解析点；保留
重命名临时运行目录后仍立即复现。未执行 factory reset，未删除镜像、容器、卷或 WSL
数据盘；本地 Docker smoke 保持未验证，需 Windows 重启或上游修复后重试。

## 2026-09-20 `8729bb14` 生产候选

公开 `main` 提交 `8729bb14` 将多官方账号入站收口为单一所有者，并在可信身份关联后统一
复用旧 OneBot 玩家绑定。完整本地回归为 `3885 passed, 7 skipped`；Ruff lint/format、生产与
测试 BasedPyright、compileall、静态仓库检查和 diff check 全部通过。公开发布工作流
`35519120670` 成功；Docker Hub 与 GHCR 指向同一 manifest digest
`sha256:c2a97fab1b97ab3412b59880a2d750f01ed47f26d141a019ce0bf9a75afab9e5`。

发布工件记录 Docker 展开大小为 `260076826` bytes，其中 `/app` 为 `4848 KiB`、Python
`site-packages` 为 `106000 KiB`、字体为 `19452 KiB`，三项均通过既有预算。应用层相对
`64ff7de7` 增长 `24 KiB`，没有新增运行依赖；依赖层和字体大小不变。

生产部署前备份配置为时间戳文件，并把 `[bot.qq_official].default_account` 设置为公众账号
别名。容器 OCI revision、精确 digest 和严格配置加载均与发布工件一致，三个官方账号进入
READY。受控群的真实成员阵容查询得到以下脱敏证据：

- 指定官方账号收到 `GROUP_MESSAGE_CREATE`，输入种类为成员提及且命令被认领；
- 另一官方账号收到同一事件，但在业务执行前由账号路由静默过滤；
- 业务只执行一次，旧 OneBot 玩家绑定被复用，进度、玩家说明和阵容图片均成功送达；
- 没有出现第二份未绑定提示，OneBot 没有发送业务回复。

该记录只复核受多账号路由和绑定收口影响的行，不把单条阵容验证扩大为 A1-A12 或整个
多轮交互矩阵。C6 仍缺独立备用 provider/key；当时 C4 仍缺普通群真实异常零泄漏负向证据；
C8 仍缺腾讯真实 receive/reject 或额度拒绝事件。该候选当时保持保守基线 5/8，后续候选的
新增证据见下文。

### 未映射群的专用账号引导

多账号唯一响应不能阻断专用账号的首次群映射。未映射群的普通全量消息仍只归默认账号；
当事件结构明确表示当前账号被 @ 时，允许该账号处理一次可识别命令并正常回复。该临时放行
本身不建立任何映射：只有 NapCat 从预声明候选数字群中观察到可信官方机器人发出的、与
待确认回复唯一一致的消息，才持久化群号与该 AppID 下 `group_openid` 的关联。映射建立后，
入站路由立即按配置的群所有权恢复单账号处理。完全未知群、模糊匹配、来源不可信和已有
映射冲突都不得写入状态库。

## 2026-09-21 `c328a103` 生产候选

公开提交 `c328a1036e13f3db118df705292e13eb0859d8c6` 将 QQ Official 群回复寻址从
“只处理一个请求的第一条消息”改为“处理该请求的每一条含文本消息”。已有显式成员目标
保持不变；纯图片不添加成员提及，因此不会生成独立 `@` 消息。聚焦回归为 137 passed，
完整回归为 3893 passed、7 skipped；Ruff、生产与测试 BasedPyright、compileall、静态
仓库检查和 diff check 全部通过。

公开发布流水线 `35528052167` 成功，Docker Hub 精确标签和 `latest` 指向 manifest digest
`sha256:9a2297e30105162e0815e136d6a4452aed7d34e96d17b749be5ece611e2d3c0f`。发布工件记录展开
大小为 `260078484` bytes，较前一生产候选减少 56 bytes；`/app` 为 `4848 KiB`、Python
`site-packages` 为 `106000 KiB`、字体为 `19452 KiB`。

Unraid 在严格配置检查通过后按精确 SHA 标签滚动替换。五个官方账号全部 READY，OneBot
静默观察连接和三个无头 worker 均恢复。受控群随后执行真实阵容查询：官方端依次送达
两条含发令成员提及、换行和正文的文本，再送达一条纯图片；序号为 `1/2/3`，NapCat
客户端观察一致，业务只执行一次。稳定检查后已删除回滚容器、仅 root 可读的临时环境与
inspect 文件和前一公开镜像标签，生产只保留一个 `ironsbot` 容器。

同一生产镜像继续完成主要查询复核。公众账号 C2C 的收集、群星牌和巅峰查询都先送达
进度，再以 `sequence=2` 送达最终文本；收集显示完整汇总和单项榜单变化说明，群星牌显示
分数、全服名次与样本百分位，巅峰在 90 秒操作窗口内保留竞技、狂野、专家和大师结果，
并把赛季榜及样本超时标成局部失败。受控群的竞技池、大师池和专家池分别被唯一认领并
各送达一张图片。幸运橱窗未在当前 digest 强行复测：公众账号 C2C 未开放该 Feature，
测试身份也不拥有生产配置中的橱窗登录账号。随后 owner 在受控群连续两次发送“橱窗”，
均收到 `1/2/0` 登录确认菜单；回复 `1` 后无头登录认证成功，腾讯返回图片送达回执，NapCat
客户端分别观察到带原生引用的 PNG。该证据在不扩大公众账号权限的前提下关闭当前 A7，
主要查询功能里程碑因此达到完成条件。随后又由已经绑定米米号的 NapCat 测试账号在同一
受控群发送“@官方机器人 橱窗”：`local_bot` 唯一认领命令并通过官方通道返回一张
`292263` bytes 的 PNG，OneBot 只观察请求和官方回复，没有发送业务消息。由于当天结果已经
缓存，本次直接复用缓存而未再次进入 `1/2/0` 登录确认；这同时证明旧 OneBot 玩家绑定可经
既有平台身份关联供官方通道复用，不存在独立的第二套玩家绑定数据库。

随后提交 `1b16e9bd` 恢复原版幸运橱窗的登录授权边界，并把缓存读取与登录授权明确拆开：
任意已获功能权限的用户可以查看 TOML 授权账号当天已经生成的公共缓存；缓存缺失时，只有
`[[seer.lucky_skin_window.accounts]]` 中分配该账号且绑定一致的用户本人，或超级管理员代查
同一授权表中的账号，才能进入登录确认。只存在于 `seer.player_accounts` 的密码不再允许超级
管理员绕过橱窗授权表。专项 `124 passed`，全量 `3894 passed, 7 skipped`，Ruff、格式、
生产与测试 BasedPyright、compileall、静态仓库检查及 diff check 全部通过。workflow
`35549339117` 成功发布 digest
`sha256:528323d058a80f5c2832a8f4e5d2c82124d90b5f13736356c07953c997afaa42`，Unraid 通过现有
模板滚动更新后五个官方账号 READY、OneBot observer 已连接、三个无头 worker 全部就绪，
且只保留一个 IronsBot 容器和一个仓库镜像。受控 NapCat 测试身份随后查询 owner 的橱窗，
服务从 SQLite 命中当天缓存并由官方通道唯一送达图片；该请求未显示 `1/2/0` 菜单，也未打开
新的无头登录会话，证明跨用户读取已有缓存不会获得登录权限。

## 2026-09-21 统一命令回复模板与 Markdown 换行

公开提交 `07004fc8` 将命令回复的引用、发令成员提及和提及后分隔符收口到一个平台无关
`ReplyTemplate`。默认命令回复引用原入站消息；群聊文本提及发令成员并在正文前换行；私聊不
提及；纯图片只保留回复上下文，不额外生成一条孤立提及；业务已经指定成员目标时不重复提及
发令者。QQ Official 便携命令与 OneBot 的便携、旧公共回复、配置文本、会议和多段消息入口
共同消费该模板，主动推送仍保持无入站引用的独立语义。完整回归为 `3898 passed, 7 skipped`，
Ruff、864 文件格式检查、生产与测试 BasedPyright、compileall、静态仓库检查和 diff check
全部通过。

workflow `35550894935` 从该提交成功发布 digest
`sha256:4482d86c9f302d71f4013736ae153cee7fd712263fc13df5051dc1af2b67cdcc`，展开大小
`260082425` bytes，`/app`、site-packages 和字体分别为 `4852 KiB`、`106000 KiB` 和
`19452 KiB`。Unraid 精确部署后五个官方账号 READY、OneBot observer 连接、三个无头 worker
健康。受控群首次实测发现腾讯 Markdown 将单个换行折叠为空格；这与官方文档“多行需要空行”
的规则一致，不是模板未执行。

提交 `41c8532f` 因此只在 QQ Official renderer 边界把模板的一个语义换行编码为空行，不改变
OneBot 的单换行表现，也不在业务命令中增加平台条件。完整发布门禁仍为
`3898 passed, 7 skipped`。workflow `35551554803` 成功发布 digest
`sha256:3830b21208cd3c0e854f1b393d45f8aa06f32770ea1a0b6f4ae4e64a654d3904`，展开大小
`260082431` bytes，`/app`、site-packages 和字体仍为 `4852 KiB`、`106000 KiB` 和
`19452 KiB`。生产精确 revision、digest 和唯一运行容器均与该工件一致；五个官方账号、
OneBot observer 和三个无头 worker 恢复健康。

同一受控群由 NapCat 发出一次明确 `@机器人 + 帮助`，官方账号只回复一次。静默观察端读取
到的原始 Markdown 含提及标签后的空行，帮助正文恰好出现一次；QQ 将该 Markdown 展开为
普通文本段时会丢弃前导空行，因此派生文本段不作为原始排版证据。Markdown 回复继续携带
原入站 `msg_id/msg_seq`，属于平台被动回复；不附加原生 `message_reference`，因为既有真实
客户端证据证明“Markdown + 原生引用”会把整段内容重复显示。也就是说，当前官方群文本选择
“单条被动回复、同条提及、正文不重复”，而不是重新引入可见引用气泡导致的双份正文。

## 2026-09-21 `623ed464` 管理异常隔离验收

公开提交 `623ed464618b233f0ef8c80b492dfd01c745eee7` 在统一管理通知出口增加脱敏投递汇总，
只记录动作名、私聊/群目标数及成功/失败数，不记录 QQ、OpenID 或会话 ID。专项验证为
`36 passed`，Ruff、格式、BasedPyright、compileall 和 diff check 均通过。workflow
`35552844162` 成功发布 Docker Hub/GHCR 共同 digest
`sha256:6fe9e98b07031a4c8c491301e2a5a9a7e17a62e2b8776127ee8e8b877e8c1d6d`；`/app`、
site-packages 和字体分别保持 `4852 KiB`、`106000 KiB` 和 `19452 KiB`。

Unraid 精确部署该 revision 后，五个 QQ Official 账号全部 READY、OneBot observer 连接、三个
无头 worker 最终全部进入游戏。启动期间一个无头 worker 真实登录失败并进入自动重连，随后
恢复成功。恢复产生的 `headless state notice` 脱敏汇总为 `private_targets=2`、
`group_targets=0`、`succeeded=1`、`failed=1`：腾讯接受唯一一条 C2C 主动通知，同一逻辑身份的
OneBot 私聊目标因 observer 静默策略被明确拒绝，没有群投递尝试。

以该状态通知为中心的 85 秒真实观察窗口内，NapCat 共观察到 38 条群消息；官方群主动回执
为 0，官方机器人管理异常关键词消息为 0。结合运行中配置的脱敏目标审计（2 个私聊超级
管理员目标、0 个 `admin_notice` 群目标），C4 的管理员送达与普通群零泄漏均在最新精确
digest 上完成，验收状态改为通过。C8 仍缺腾讯真实 receive/reject 或额度拒绝事件，登记为
非阻塞外部 TODO，不再阻止继续关闭其余可控验收项，也不得被描述为已经取得对应真实证据。

## 2026-09-21 外部 TODO 重新分类与 C6 验收

- 第一套候选 provider 最初只返回视频模型；权限调整后虽能列出文本模型，但两次最小
  非流式聊天请求均未在可用时限内返回，其中一次明确在 55 秒读取超时，因此未接入配置。
- 第二套独立 provider 的模型目录、最小 completion 和 IronsBot 正式 failover 均已真实
  验证。主 provider 返回 HTTP 401 后，客户端跳过其剩余模型并切换到 NoMiss
  `gpt-5.6-luna`，最终返回 HTTP 200、`provider=nomiss` 和唯一 `FAILOVER_OK` 文本。结合
  既有 75 项 AI/主动投递专项门禁及 C4 普通群零泄露实机证据，C6 改为通过。
- C8 已有真实主动消息成功回执、客户端显示、退订抑制和错误分类门禁。腾讯何时产生
  receive/reject 或额度耗尽由平台和真实接收状态决定，项目不能安全伪造；该证据发生后补录。
- C8 不阻塞当前版本的产品验收，继续列入最终报告的 External TODO。B10 已由用户完成
  手机 QQ 最终视觉确认，Phase 7 里程碑计数为 8/8。

## 2026-09-21 查询静默与 B站失败告警收口

真实群聊暴露出两项不改变命令语义、但会造成额外打扰的问题。提交 `374ef4d5` 将空实体
查询的处理收口到共用 `PortableQuerySessions`：精灵、立绘、头像、刻印和宝石仍由各自
精确解析器认领，找到唯一结果或候选时正常回复；服务返回空 `QueryResult` 时 OneBot 与
QQ Official 都静默结束，且已认领输入不会继续进入 AI。回归覆盖“币斯奥技能”、普通聊天
中包含“刻印”的句子、单独“刻印”和“一个刻印”，并验证两平台适配器均不发送消息。

提交 `c9b989ba` 将同一 B站动态的正文和图片失败先汇总，再按类型化会话稳定去重，最后只
通过一次 `AdminNoticeService` 调用通知管理员。正文和图片仍独立投递，部分成功语义不变；
变化仅是两段都失败时不再产生两条相同告警。生产配置、TOML、env、SQLite 和 Unraid 模板
均无需迁移。

生产改动完成后全量回归为 `3905 passed, 7 skipped`；最终 OneBot 静默适配测试补齐后，
相关专项为 `114 passed` 和 `79 passed`，Ruff、生产与测试 BasedPyright、compileall、静态
仓库检查及 diff check 均通过。公开 workflow `35557100608` 随后从提交 `2f1dd988` 成功
发布 Docker Hub/GHCR 共同 digest
`sha256:82576bcd9a8ced374acfe3432a89d2629855747daf510cdd0df82d1564dd526b`，固定标签为
`0.5.1.1953` 与 `sha-2f1dd98`。Docker 展开大小为 `260083155` bytes，较上一发布候选仅
增加 `394` bytes；`/app`、site-packages 和字体分别为 `4852 KiB`、`106000 KiB` 和
`19452 KiB`，构建、离线 smoke、依赖审计、目录与增长门禁全部通过。该候选尚未部署到
生产 Unraid，因此不增加 Phase 7 实机里程碑；部署后只需复核受影响的查询静默与单次管理
告警，不重开已经关闭的 C4/C6。

## 2026-09-21 `957a564e` 跨端成员关联候选

公开提交 `e87592ae` 将同一条群消息在 QQ Official 与 OneBot/NapCat 两端的结构化身份
证据接入既有 `SilentIdentityObservationService`。官方事件提供发送者和有序的
`member_openid`，NapCat 提供发送者和有序的数字 QQ；只有 AppID、逻辑群、规范化正文、
时间窗和目标数量唯一一致时才建立关联。发送 `米米号 @成员` 等已认领命令可以同时关联
发令者和目标成员；NapCat 辅助账号可发送纯 `@官方机器人 @目标成员` 完成无文字验证。
后者必须同时包含已配置机器人和至少一个目标，单独 @ 普通成员不会写入身份库。

提交 `957a564e` 进一步允许群成员只发送空的 `@机器人` 来关联自身发送者身份。该路径仍
要求两端观察到同一条消息；未寻址空事件、歧义、过期、自身消息缺少目标、可信官方机器人
作为发送者及已有冲突都拒绝写入。关联只把 AppID 作用域的端点挂到统一 QQ 主体；米米号、
权限和其他业务状态继续复用该主体的既有数据，不把不同 AppID 的 OpenID 当成可互换发送
端点。TOML、env、SQLite schema 和 Unraid 模板均无需迁移。

发布候选全量门禁为 `3919 passed, 7 skipped`；Ruff、864 文件格式检查、生产与测试
BasedPyright、compileall、静态仓库检查和 diff check 全部通过。公开 workflow
`35561719600` 成功发布标签 `0.5.1.1956` 与 `sha-957a564`，Docker Hub/GHCR 共同 digest
为 `sha256:2463d6e80b40e46187255bbad2baa43acb3e5d28edf353f7920088a1b4238e8c`。
镜像展开大小为 `260093622` bytes，较 `2f1dd988` 增长 `10467` bytes（10 KiB）；`/app`、
site-packages 和字体分别为 `4860 KiB`、`106000 KiB` 和 `19452 KiB`。构建、离线 smoke、
依赖审计、目录预算和镜像增长门禁均通过。该精确候选尚未部署到生产，因此上述三种新关联
路径仍需在真实群里复核，不增加 Phase 7 的 7/8 里程碑计数。

## 2026-09-21 `037ca6df` NapCat 自发身份观察修复与生产验收

首次将 `957a564e` 部署到生产后，真实 NapCat 辅助验证暴露出一处入口缺口：辅助账号通过
本地 OneBot HTTP API 发送 `@官方机器人 @目标成员` 时，NapCat 将其上报为
`post_type=message_sent`，而旧 ingress 只把 `GroupMessageEvent` 交给身份观察器。官方端已
收到同一消息并正常回复，但 OneBot 事件在进入 `SilentIdentityObservationService` 前被
静默 gate 丢弃，因此没有建立关联。该失败证据证明服务层允许自发验证的单元测试不足以
代表真实 NapCat 事件入口已经接通。

提交 `037ca6df` 将群 `message_sent` 解析成现有 `OneBotGroupMessageObservation`，随后仍由
原静默 gate 抛出 `IgnoredException`；该事件不会进入 matcher、AI 或普通业务服务，也没有
新增第二套关联算法。回归测试直接使用 OneBot adapter 对真实事件形状的解析结果，并证明
观察完成后事件仍被阻断。TOML、env、SQLite schema、Docker 参数和 Unraid 模板均无需迁移。

公开 workflow `35565064278` 成功发布 `0.5.1.1959` 与 `sha-037ca6d`，Docker Hub/GHCR
共同 digest 为
`sha256:6fee32a81cd79574c5bf6e69a7f3720545d130306795f205f3a19b8f203cb398`。镜像展开大小为
`260095346` bytes，较 `957a564e` 增长 `1724` bytes；`/app`、site-packages 和字体分别为
`4860 KiB`、`106000 KiB` 和 `19452 KiB`。构建、离线 smoke、依赖审计、目录预算和增长
门禁全部通过。

生产实机验收先使用 SQLite 在线备份保存状态库，再仅删除用户自有第二 NapCat 测试账号在
`local_bot` 下的一条成员端点映射。辅助账号发送纯 `@官方机器人 @目标成员` 后，OneBot
收到 `message_sent`，QQ Official 收到同一 `GROUP_MESSAGE_CREATE`，服务记录一次脱敏的
`silent identity link confirmed`；数据库恢复为恰好一条映射，恢复后的 OpenID 与群作用域
摘要均和删除前一致。随后发送带目标成员的米米号查询，`local_bot` 唯一认领并完成被动
回复，NapCat 客户端观察到目标 QQ 已有米米号的完整玩家信息，证明业务绑定由统一 QQ 主体
复用而不是复制或猜测 OpenID。验收期间 OneBot 没有发送业务回复，错误计数为零。

最终全量验证为 `3920 passed, 7 skipped`；Ruff、864 文件格式检查、生产与测试
BasedPyright、compileall、静态仓库检查和 diff check 全部通过。NapCat 辅助目标关联已经
闭环；该记录形成时，群成员空 `@机器人` 与群成员 `@机器人 + 命令` 的发送者关联仍等待
真实人类账号事件，不能由辅助账号自发消息伪造。后续真实事件与 B10 客户端确认均已补齐。

## 2026-09-21 `a2ac4e5c` 多账号群推送唯一归属修复

生产实机发现同一逻辑管理群同时被两个官方机器人主动推送。审计确认 TOML 只为该群声明
一个静态官方端点，但静默身份观察又为同群发现了默认账号端点；入站路由能够按群归属拒绝
默认账号，Feature 和 B站动态目标却仍把两个动态端点都登记为主动目标。问题不是群 Feature
配置混乱，也不是两个容器同时运行，而是主动出站缺少与入站相同的账号归属门禁。

提交 `a2ac4e5c` 让 QQ Official 主动出站复用同一个 `QQOfficialIngressRouting`：
`group_routes` 或“唯一静态端点”选出的账号是该逻辑群唯一主动发送者，其他同群机器人即使
观测到 OpenID 也不会登记为动态推送目标；出站 messenger 另有最终门禁，防止旧状态或静态
多端点绕过。该规则只限制主动发送，用户直接点名非归属机器人时仍可通过收到事件的 AppID
完成被动回复。TOML schema、env、SQLite schema 和 Unraid 模板均未改变。

专项回归覆盖两个 AppID 同群时“归属账号可主动发送、另一账号拒绝主动发送但仍可被动
回复”，相关测试为 `145 passed`；最终全量为 `3921 passed, 7 skipped`，Ruff、864 文件格式
检查、生产与测试 BasedPyright、compileall、静态仓库检查及 diff check 全部通过。公开
workflow `35569300875` 成功发布 `0.5.1.1960` 与 `sha-a2ac4e5`，Docker Hub/GHCR 共同 digest
为 `sha256:96961abeb431de2fa0041e7ffd7b765029fcc2cfb9759d1641e71aed9caf4e82`，生产容器运行
revision 与该 digest 一致，五个官方账号 READY，启动后错误计数为零。

同轮配置审计发现 `youqijiang` 已有严格验证的群映射与原有 Feature，但缺少专用账号路由，
因此 `youqi_bot` 的入站被确定性拒绝。生产 TOML 先备份，再只增加
`youqijiang = "youqi_bot"`；严格配置检查通过，没有修改任何 Feature。重启后使用 NapCat
分别在 `youqijiang` 和 `diexia1` 发送 `@机器人 帮助`，两条事件均由各自账号唯一认领，
腾讯均返回一次成功被动投递。管理群下一条自然主动推送仍需客户端确认只出现一次；在此
之前不把本项写成完整客户端闭环，也不增加 Phase 7 的 7/8 里程碑计数。

## 2026-09-21 `1a4b8230` 私聊主动端点回归修复

`a2ac4e5c` 首次生产启动时暴露出一项边界回归：账号选主门禁同时作用于群聊和私聊，导致
非默认官方账号已经取得的 AppID 作用域 C2C 端点被误判为未选中。群聊确实必须按逻辑群选出
唯一主动发送账号；私聊端点却天然属于取得该端点的 AppID，不能由全局默认账号替代，也不应
因为它属于非默认账号而失效。

提交 `1a4b8230` 将主动发送选主严格收窄到群会话：群推送继续执行唯一账号归属门禁，已知的
官方私聊端点继续由其所属账号发送。新增回归测试覆盖非默认账号私聊主动投递，专项为
`44 passed`；最终全量为 `3922 passed, 7 skipped`，Ruff、864 文件格式检查、生产与测试
BasedPyright、compileall、静态仓库检查及 diff check 全部通过。

公开 workflow `35570402550` 发布 Docker Hub/GHCR 共同 digest
`sha256:e90a843e39c938f5c75e4c9d7aac210a813115c1f45632ab3696049f990de6bc`，标签为
`0.5.1.1962` 与 `sha-1a4b823`。Unraid 更新后五个官方账号 READY、OneBot observer 已连接、
三个无头 worker 全部健康且应用正常启动。真实启动通知、数据同步通知和 Docker 更新通知均
显示 `private_targets=2`、`group_targets=0`、`succeeded=1`、`failed=1`：官方 C2C 主动消息
由腾讯接受，同一逻辑身份的 OneBot 私聊端点因静默策略明确跳过，管理群没有被作为通知目标。
管理群下一条自然业务推送仍需客户端确认只由路由指定的一个官方机器人显示。

## 2026-09-21 `676f99ee` 模糊实体查询全族修复与主动推送复核

精灵、刻印和属性查询先后出现过普通语句被前后缀命令认领后回复“未找到”的问题。根因不在
三个独立 parser，而在 QQ Official portable 注册层为每个实体操作分别配置空结果文案；此前
修复只覆盖当时发现的操作，未形成共享策略。提交 `676f99ee` 审查并统一了精灵、立绘、头像、
刻印、宝石、套装、部件、称号、属性、异常、精灵配置和群星牌的空结果行为：模糊语法已认领
但实体库无匹配时静默结束，不发送“未找到”，也不继续交给 AI；数据源异常、已展示菜单中的
过期选择等真实错误仍明确反馈。全量回归为 `3929 passed, 7 skipped`。

公开 workflow `35579126654` 发布 `0.5.1.1966` 与 `sha-676f99e`，Docker Hub/GHCR 共同
digest 为 `sha256:a48f1c892c8ac5c50ed43b3c6b05cb1a538e81dda50b4cd8cabde3488085ca4b`，
生产运行 revision 与该 digest 一致，展开大小为 `260096650` bytes。五个官方账号、OneBot
observer、三个无头 worker 和应用启动均正常。

为复核多账号群主动推送唯一归属，生产 TOML 先创建精确备份，再临时添加仅管理群可见且
只执行一次的定时文本。腾讯接受一次群主动投递，NapCat observer 只观察到一条对应群消息；
同轮另有一次超级管理员 C2C 投递，OneBot 群与私聊端点均按静默策略明确跳过。测试后已恢复
原 TOML，严格配置检查通过，临时 feature 与 schedule 均不存在，容器重新完成启动。该证据
关闭“同一管理群被两个官方账号主动推送”的服务端和观察端复核；该记录发生时 B10 手机 QQ
最终视觉确认仍未完成，后续已由下面的最终验收记录关闭。

后续提交 `a6961d39` 首先验证了共享策略可以消除各注册点重复配置；继续对照原版 OneBot
链路后，最终实现没有保留新增的 portable 专用模板模块。既有 `AffixCommand` 直接提供
参数提取入口，既有 `QueryOperationSpec` 默认采用与原版一致的空结果静默，QQ Official
仍走通用 `build_query_operation`。共享会话模块继续只负责会话状态，没有放宽文件大小
守护或增加第二套查询 API。专项为 `103 passed`；TOML、env、Docker、SQLite 和 Unraid
配置均不改变。

最终提交 `ae186314` 删除临时新增的 portable 专用模板文件，完整回归为
`3930 passed, 7 skipped`。workflow `35583061598` 发布 `0.5.1.1970` 与
`sha-ae18631`，Docker Hub/GHCR 共同 digest 为
`sha256:a629bb7be846c32db312f632e4e0a1d6a337897bb47572cc80db656df42be87a`；镜像大小
`260096148` bytes，比上一候选减少 `234` bytes，`/app`、site-packages 和字体分别为
`4860 KiB`、`106000 KiB` 和 `19452 KiB`。生产更新前备份容器定义，更新后严格配置校验
通过，五个官方账号 READY、三个无头 worker 健康、OneBot observer 已连接。

更新后的真实群流量包含“是不是1000RMB一个？”和“低等属性”等旧解析器容易误认的普通
语句；后者包含完整属性后缀。NapCat 正常观察到消息，QQ Official 没有发送 payload，日志
也没有“未找到对应属性/刻印/精灵”，证明空结果静默在生产链路生效。启动时 GitHub commit
说明接口触发公开 API 限流，远端数据库同步遇到一次连接失败；两项均按设计降级，现有数据、
官方连接、无头 worker 和应用启动未受影响。

## 2026-09-21 B10 手机 QQ 最终视觉确认与 Phase 7 关闭

用户在手机 QQ 对当前生产候选完成最终视觉检查并确认“显示正确无误”：回复中的发令成员
提及与正文位于同一条消息，提及后正确换行，正文只出现一次，没有单独的提及占位消息或
字面协议标签。服务器侧 `17:45:26` 记录同一 `local_bot` 群命令被识别，`17:45:27`
仅投递一条 `mode=passive`、`sequence=1` 的 `QQOfficialTextPayload`；容器复核仍运行
`ae186314a659d3cabbfcb8dfe3b00e127c8fefc7`。

该客户端证据关闭 B10 和 Phase 7 最后一个实机里程碑。Program 为 8/8，Phase 7 为 8/8。
C8 所指腾讯自然产生的 receive/reject、频控或主动消息额度拒绝仍是非阻塞 External TODO；
本验收没有把未发生的平台事件写成通过，也不需要为制造拒绝而影响真实用户。
