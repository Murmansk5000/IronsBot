# QQ 官方机器人可用性验收

2026-09-15 用户重新明确优先目标：先在官方机器人上可用，不继续以结构重构代替使用验收。
旧的固定 98% 停止使用。完整多平台目标仍保留，但不得用 OneBot 内部清理阻塞官方单账号验收。

## 当前结论

基础接入已有真实使用证据，不能说机器人完全不可用；最新代码的完整日用能力尚未验收。
本机进程启动于 12:52:06，最新业务提交 c26ba863 时间为 17:41:49。
进程存在不等于健康，也不证明已载入后续提交。本次没有重启进程或发送测试消息。
最近全量本地结果为 3728 passed、7 skipped；这不是 QQ 客户端功能验收结果。

## 验收进度

按以下 8 项验收里程碑计数：4/8 有完成记录，50%。
这是等权的验收项完成率，不是代码完成率、剩余工时估计或最新版回归通过率。
前四项是历史实机证据，更新部署后的回归仍需确认。后续不因增加提交数提高百分比。

| 项目 | 状态 | 证据或完成要求 |
| --- | --- | --- |
| SDK 登录、READY、自动令牌更新 | 历史实机通过 | protocol-baseline 中的登录与令牌轮换记录 |
| 官方私聊及群 @ 基础命令 | 历史实机通过 | 私聊帮助、群 @ 命令各返回一次的记录 |
| 图片上传、显示和连续回复 | 历史实机通过 | 群和私聊图片显示确认，连续序号记录 |
| 重连 Resume、正常关闭 | 历史实机通过 | 隔离连接重连和正常关闭记录；腾讯重复重投仍未观察到 |
| 最新版本本机部署与健康回归 | 待完成 | 明确运行提交；新进程 READY；无启动异常；基础命令和图片复验 |
| 主要查询功能 | 待完成 | 官方端逐项验收玩家信息、收集、巅峰、群星牌、阵容、榜单、竞技/大师/专家池、精灵/立绘、刻印、新增内容和帮助；不可用项说明具体原因 |
| 多轮交互和用户可见行为 | 待完成 | 二级帮助、候选选择、绑定确认、连续详情、退出、超时与快速回复；文本流程必须可用，按钮按已授权能力验收 |
| 权限、故障及实际配置功能 | 待完成 | 黑名单、功能开关、管理异常不外泄，以及启用的 AI/推送等实际配置功能；通过受控测试验证，不在普通群发送诊断消息 |

## 接下来执行

1. 核对本机启动方式，启动最新版本，记录运行提交和脱敏健康证据。
2. 先进行无需用户操作的检查，再给用户一份合并后的最小测试口令清单。
3. 记录每条官方实测结果：输入、预期、实际、对应运行提交；发现阻碍使用的问题就修复。
4. 仅在复用业务或修复官方功能确有需要时继续抽象模板，不再先做无关结构整理。
5. 未完成项保留明确状态，不以 mock、测试数量或口令声明数量推定实测完成。

第二 AppID、多账号实机隔离、未授权主动消息测试和自定义键盘权限继续保留在完整目标中。
这些不等于单账号基本查询不可用，也不能伪报已通过。未经要求不 push，不暴露凭据。
